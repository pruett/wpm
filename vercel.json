{
  "buildCommand": "bun run build:api && if [ \"$VERCEL_ENV\" = \"production\" ]; then bun src/utils/commands.ts; fi",
  "outputDirectory": "public",
  "crons": [
    {
      "path": "/api/sync",
      "schedule": "*/5 * * * *"
    }
  ]
}
