{
  "name": "wpm",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "predev": "kill $(lsof -ti :4100 -i :4101) 2>/dev/null; true",
    "dev": "turbo dev",
    "test": "turbo test",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt --write .",
    "fmt:check": "oxfmt --check ."
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "oxfmt": "^0.36.0",
    "oxlint": "^1.58.0",
    "turbo": "^2.9.3"
  },
  "packageManager": "bun@1.3.11"
}
