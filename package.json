{
  "name": "wpm",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "test": "turbo test",
    "clean": "turbo clean && rm -rf node_modules bun.lock",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "fmt": "oxfmt --write .",
    "fmt:check": "oxfmt --check ."
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "oxfmt": "^0.36.0",
    "oxlint": "^1.58.0",
    "turbo": "^2.9.3"
  },
  "packageManager": "bun@1.3.11"
}
