{
  "name": "@wpm/web",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'TODO: configure web build'",
    "dev": "echo 'TODO: configure web dev server'",
    "test": "vitest run"
  },
  "dependencies": {
    "@wpm/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
