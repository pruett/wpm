## 2026-05-11 — Phase 0 Slice 1: Add `events` table to schema

Added the new `events` pgTable to `src/lib/db/schema/app.ts` with columns per PRD §Schema:
- `id` (text PK, will be derived from Kalshi `event_ticker`)
- `sport` (text enum from SPORTS)
- `name` (text)
- `closesAt` (bigint, number mode)
- `status` (text enum: 'open' | 'terminal')
- `createdAt` (bigint, number mode)
- Index `ix_events_status_closes` on (status, closesAt)

Also added `eventRelations` declaring one-to-many `event → markets` (FK + many side will be wired into `markets` in a later task).

Old columns on `markets`/`ammPools`/`positions` deliberately left intact so the codebase still compiles. Schema-level changes (column reshape, FK, enum drops, migration regen) come in subsequent plan tasks.

### Files changed
- `src/lib/db/schema/app.ts`

### Verification
- `bunx tsc --noEmit` — only pre-existing `vitest.integration.config.ts` overload error remains (unrelated; present before this change).
- `bunx oxfmt --check src/lib/db/schema/app.ts` — clean.
- Lint warnings in repo are all pre-existing in tests, not from this change.

## 2026-05-11 — Phase 0 Slice 1: Reshape `markets` (additive)

Added new columns to the `markets` pgTable in `src/lib/db/schema/app.ts` per PRD §Schema:
- `eventId` (text, FK to `events.id` with `onDelete: cascade`) — links each Market to its parent Event
- `ticker` (text, nullable) — Kalshi market ticker
- `resolvedAs` (text enum: 'yes' | 'no', nullable) — replaces `resolvedOutcome` for YES-only product

Wired `marketRelations` with `event: one(events, { fields: [markets.eventId], references: [events.id] })` so the one-to-many `event → markets` join is reachable from both directions.

Followed the Slice 1 strategy established in the prior task: additive changes only. The old A/B columns (`sport`/`teamA`/`teamB`/`tickerA`/`tickerB`/`closesAt`/`resolvedOutcome`) remain in the schema so the 13 downstream consumers (translator, settlement, AMM, trading/positions/markets data layer, portfolio/bet-controls UI) still compile. Actual drops are deferred to Phase 1 once consumers are rewritten.

### Files changed
- `src/lib/db/schema/app.ts`

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bunx oxfmt --check src/lib/db/schema/app.ts` — clean.
- `bunx oxlint src/lib/db/schema/app.ts` — 0 warnings, 0 errors.
- `bun run test` — 48/48 vitest unit tests pass (settlement, amm, translator).

### Notes
- `eventId` is intentionally nullable in the schema right now: while old A/B columns coexist, existing seeded markets can't satisfy a NOT NULL FK. The migration regen in Phase 1 will tighten this once the full reshape lands and the table is wiped.
- `name` already existed on `markets` before this task — no add needed.

## 2026-05-11 — Phase 0 Slice 1: Regenerate migration

Regenerated the single drizzle migration so it reflects the current additive schema state (new `events` table + `eventId`/`ticker`/`resolvedAs` on `markets` + `reserveYes`/`reserveNo` on `ammPools` + `shares` on `positions`, all alongside the legacy A/B columns).

Procedure:
1. Deleted the old `0000_giant_triton.sql`, `meta/0000_snapshot.json`, and `meta/_journal.json`.
2. Recreated `meta/_journal.json` with an empty `entries: []` array so `drizzle-kit generate` could compute the prevId/idx for a fresh first-migration emit. Without this stub, drizzle-kit panics with `ENOENT` on the journal file.
3. Ran `bun run db:generate`, which produced `0000_petite_grey_gargoyle.sql` plus updated snapshot+journal. The wrapper script `append-to-migrations.ts` then appended the treasury seed `INSERT` at the tail of the SQL — verified present.
4. Ran `oxfmt --write` on the regenerated `_journal.json` to match the project's trailing-newline expectation (drizzle-kit emits without one; pre-existing snapshot in the repo had been re-formatted at some prior point).

The new migration intentionally still creates the legacy `team_a`/`team_b`/`ticker_a`/`ticker_b`/`closes_at` (on `markets`), `reserve_a`/`reserve_b`, and `shares_a`/`shares_b` columns. Slice 1 is additive — the actual drops happen in Phase 1.

### Files changed
- `src/lib/db/migrations/0000_giant_triton.sql` (deleted)
- `src/lib/db/migrations/0000_petite_grey_gargoyle.sql` (new)
- `src/lib/db/migrations/meta/0000_snapshot.json` (regenerated)
- `src/lib/db/migrations/meta/_journal.json` (regenerated)

### Verification
- `bunx tsc --noEmit` — only pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 48/48 vitest unit tests pass.
- `bun run lint` — 2 pre-existing warnings in `tests/integration/bet-and-resolve.test.ts`; 0 introduced by this change.
- `bun run fmt:check` — 31 pre-existing format issues (same count as before regen, after formatting `_journal.json`).

### Gotcha
- `drizzle-kit generate` will not run if `meta/_journal.json` is absent — it tries to read the file before it figures out it's a first-migration scenario. Workaround: stub the file with `{"version":"7","dialect":"postgresql","entries":[]}` before running generate. drizzle-kit overwrites it on success.

## 2026-05-11 — Phase 0 Slice 1: Reshape `ammPools` & `positions` (additive)

Added the new YES-oriented columns to the `ammPools` and `positions` pgTables in `src/lib/db/schema/app.ts` per PRD §Schema:
- `ammPools.reserveYes` (bigint, nullable) — YES-side reserve, replaces `reserveA`
- `ammPools.reserveNo` (bigint, nullable) — NO-side reserve, replaces `reserveB`
- `positions.shares` (bigint, default 0) — collapsed YES-only share count, replaces `sharesA`/`sharesB`

Continued the Slice 1 additive strategy established in prior tasks: the new columns sit alongside `reserveA/B` and `sharesA/sharesB` so the existing AMM/settlement/trading/positions consumers still compile. The actual drop of the old columns is deferred to Phase 1 (per PLAN §Phase 1 "Delete obsolete columns from `app.ts`"), where the migration regen will wipe and recreate the affected tables.

`SellShares` is also deliberately left in `transactionTypes` for now. Dropping that enum value would immediately break `src/data/trading.ts:sellShares` and `src/actions/sellShares.ts`, both of which are slated for deletion in a later Slice 1 task ("Update placeBet server action … delete `src/actions/sellShares.ts`"). The drop will land together with the consumer removal so we never have a partial-compile state.

### Files changed
- `src/lib/db/schema/app.ts`

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bunx oxfmt --check src/lib/db/schema/app.ts` — clean.
- `bunx oxlint src/lib/db/schema/app.ts` — 0 warnings, 0 errors.
- `bun run test` — 48/48 vitest unit tests pass (settlement, amm, translator).

## 2026-05-11 — Phase 0 Slice 1: Rewrite `translateKalshiEvent` happy path

Reshaped the translator's output to the new multi-outcome model: a single Kalshi Event now produces one `TranslatedEventRow` plus N `TranslatedMarket` rows (each a YES-side wampum Market) instead of one collapsed A/B Market.

Key changes to `src/lib/kalshi/translator.ts`:
- New return shape: `{kind: 'ok', value: {event, markets: TranslatedMarket[]}}`.
- `TranslatedEventRow = Omit<events.$inferInsert, "status" | "createdAt">`.
- `TranslatedMarketRow = Omit<markets.$inferInsert, "status" | "createdAt" | "eventId">`. `eventId` is set during DB insertion by the consumer.
- Per-child `initialProbabilityYes` (renamed from the pair-averaged `initialProbabilityA`) sourced from each child's own YES-side bid/ask midpoint.
- Dropped the `non_binary` reject — translator now iterates `event.markets` instead of destructuring `[a, b]`.
- Dropped the `pair_inconsistent` reject — under the new model each child is an independent YES/NO contract with no cross-sibling check.
- `InsufficientConfidenceReason` is now `{ ticker, reason: 'spread_too_wide' }` per failing child, aggregated across siblings (still all-or-nothing at the Event level).
- `event.id = kalshi-${event_ticker}`; `market.id = kalshi-${market.ticker}` per child.
- Legacy `teamA`/`teamB`/`tickerA`/`tickerB` columns on `markets` are still NOT NULL during Slice 1's additive transition, so each translated child Market populates them with placeholder values (`teamA = yes_sub_title`, `teamB = ""`). They'll be dropped in Phase 1.
- `translateKalshiResolution` left intact (it's replaced by `decideEventCommit` in Phase 3).

### Consumer adaptation
`createMarket` in `src/data/markets.ts` is rewritten to `createEvent` in the next plan task, but it has to keep compiling until then:
- Inlined a `LegacyCreateMarketInput` type so `data/markets.ts` no longer imports `TranslatedMarket` from the translator. The function body still does the pre-multi-outcome single-row write.
- In `src/lib/kalshi/ingest.ts`, added a thin Slice 1 shim: take the new translator output, pick the first two child markets, and collapse them back into the legacy A/B input for `createMarket`. The shim is explicitly marked transitional and will be removed when `createEvent` lands.
- Removed `non_binary` from `SkipReasonKind` and the empty-counters initializer.

Translator tests rewritten (`src/lib/kalshi/translator.test.ts`):
- Happy path now asserts one `event` row + N child `markets` rows, including per-child `initialProbabilityYes`.
- Removed `non_binary` and `pair_inconsistent` cases (fixtures stay around for now — deletion is in Phase 6).
- `insufficient_confidence` test now matches the new per-child reason shape `{ ticker, reason: 'spread_too_wide' }`.

### Files changed
- `src/lib/kalshi/translator.ts`
- `src/lib/kalshi/translator.test.ts`
- `src/lib/kalshi/ingest.ts`
- `src/data/markets.ts`

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 45/45 vitest unit tests pass (down from 48; 3 removed test cases: `non_binary`, `pair_inconsistent`, and the redundant pair-averaging assertion).
- `bunx oxlint` on changed files — 0 warnings, 0 errors. `bun run lint` shows 2 pre-existing warnings unrelated to this change.
- `bunx oxfmt --check` on changed files — clean.

### Gotcha
- `TranslatedMarketRow = Omit<MarketInsert, "status" | "createdAt" | "eventId">` must omit `eventId` because the DB layer wires it during insertion (it's the FK to the parent event row created in the same transaction). Including it in the translator output type would force the translator to know its own event ID at row-construction time, which it already does — but keeping the consumer responsible for that wiring matches the pattern used in Phase 1's `createEvent`.
- The integration test `tests/integration/bet-and-resolve.test.ts` still consumes the legacy `createMarket` shape directly; its `LegacyCreateMarketInput` type is wide enough to accept that shape. It'll be rewritten in Phase 6.

## 2026-05-11 — Phase 0 Slice 1: Add `createEvent` in `data/events.ts`

Created `src/data/events.ts` with the new `createEvent` function that consumes the translator's `TranslatedEvent` shape (`{event, markets[]}`) and persists one `events` row + N `markets` rows + N `ammPools` rows in a single DB transaction. Per-Market `initializePool` seeding debits the treasury once per child Market and emits one `CreateMarket` transaction row per child.

`ingest.ts` now calls `createEvent(result.value)` directly, replacing the Slice 1 A/B-collapsing shim that wrapped the legacy `createMarket`. The ingest driver is now N-Market-capable for the happy path (Slice 2 still needs the rejection variants `too_many_markets` and `inconsistent_close_times`).

### Mapping legacy AMM reserves to YES-first reserves
`initializePool` (still in legacy A/B form during Slice 1) returns `sharesA`/`sharesB`. Under the YES-first model, `sharesA → reserveYes` and `sharesB → reserveNo`. The mapping holds because the legacy `calculatePrices` defines `priceA = sharesB / total` — so feeding `initialProbabilityYes` directly as the `initialProbabilityA` argument makes `priceA = priceYes` and therefore the "A" reserve is the YES reserve. The `ammPools` insert populates both legacy (`reserveA/B`) and new (`reserveYes/No`) columns to keep downstream consumers compiling until Phase 1 drops the legacy pair.

### Legacy `createMarket` retention
`createMarket` in `src/data/markets.ts` is kept (not deleted) because `tests/integration/bet-and-resolve.test.ts` still imports and calls it. The integration test is slated for rewrite in Phase 6; removing `createMarket` now would break the typecheck on a file outside the Slice 1 scope.

### Files changed
- `src/data/events.ts` (new)
- `src/lib/kalshi/ingest.ts`

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 45/45 vitest unit tests pass.
- `bunx oxlint src/data/events.ts src/lib/kalshi/ingest.ts` — 0 warnings, 0 errors.
- `bunx oxfmt --check` on changed files — clean (after `oxfmt --write` on `events.ts`).

## 2026-05-11 — Phase 0 Slice 1: Rewrite `placeBet` (YES-only)

Reshaped `src/data/trading.ts` so the production `placeBet` function follows the multi-outcome YES-only model (ADR-0007). Public signature is now `(input: {marketId, amount})` — the outcome parameter is gone.

Key behaviors of the new `placeBet`:
- Reads the pool's `reserveYes`/`reserveNo` columns first, falling back to `reserveA`/`reserveB` for any rows still using only the legacy columns. Under the YES-first mapping established in `createEvent`, A ↔ YES and B ↔ NO.
- Internally still calls the legacy `calculateBuy(pool, "A", amount)` because the AMM rewrite (drop `outcome` param) is scheduled for Phase 1. This task does not touch `lib/amm.ts`.
- Dual-writes the pool update: both `reserveA/reserveB` (legacy) and `reserveYes/reserveNo` (new) are set from `newPool.sharesA/B`, keeping the two views aligned during the additive transition.
- Writes both `positions.sharesA` (legacy, since A=YES) and `positions.shares` (new flat column). `sharesB` stays 0 because users only ever hold YES shares now.
- Drops `outcome` from the `PlaceBet` transaction payload (legacy `sellShares` still records it).

### Legacy adapter for the integration test
`tests/integration/bet-and-resolve.test.ts` is slated for rewrite in Phase 6 but must keep compiling now. To avoid touching its logic, added `placeBetLegacy({marketId, outcome, amount})` to `data/trading.ts` — verbatim copy of the prior `placeBet` body. The integration test imports it via a rename: `const { placeBetLegacy: placeBet } = await import("@/data/trading")`. Same pattern that `createMarket` follows in `data/markets.ts`.

### Server action
`src/actions/placeBet.ts` passes `{marketId, amount}` to the DAL instead of `parsed.data` (which still contains an outcome field). The Zod schema continues to validate `outcome` for now; the next plan task — "Update placeBet server action: drop outcome from Zod schema; delete sellShares.ts" — strips it cleanly. Minimal change to keep typecheck green between the two tasks.

### Files changed
- `src/data/trading.ts`
- `src/actions/placeBet.ts`
- `tests/integration/bet-and-resolve.test.ts`

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 45/45 vitest unit tests pass.
- `bun run lint` — 2 pre-existing warnings (same as before this change).
- `bunx oxfmt --check` on changed files — clean.

### Notes
- `bet-controls.tsx` still passes `outcome` to the server action, but that's a `string` field on a Zod schema that still accepts it, so typecheck stays green. The UI purge of A/B controls is Phase 5 work.
- The legacy `sellShares` data-layer function is unchanged. ADR-0007 deletes it, but the plan keeps it alongside `placeBetLegacy` until the integration test and `actions/sellShares.ts` are removed in later tasks.

## 2026-05-11 — Phase 0 Slice 1: Add `decideEventCommit` pure decision core

Added `decideEventCommit(wampumEvent, kalshiResponse, now)` to `src/lib/kalshi/resolve.ts` as the pure decision function that replaces per-child `translateKalshiResolution` under the multi-outcome model. Returns either `{ kind: 'wait' }` or `{ kind: 'commit', perChild: ChildOutcome[] }`.

Slice 1 scope is the happy path only — every child must be terminal, otherwise the function returns `wait`. Mapping is per-child:
- Kalshi `result: "yes"` → `resolved_yes`
- Kalshi `result: "no"` → `resolved_no`
- Kalshi `result: "scalar"` → `cancelled_scalar`
- Unrecognised result on a terminal market → `wait` (driver retries; Phase 3 will tighten this into an explicit error path)

Event-level void semantics (all children `no` → `cancelled_voided`) and deadline degradation (`cancelled_no_settlement` past 48h) are intentionally deferred to Phase 3 per the plan's explicit "Slice 1 happy path" framing. Both variants are already present in the `ChildOutcome` union so the function signature is forward-compatible.

### Matching wampum children to Kalshi markets
The function takes a minimal `WampumEventForDecision` shape (`{id, closesAt, markets: {id, ticker}[]}`) instead of a full DB row to keep it pure and unit-testable. Matching is by Kalshi market `ticker` (the wampum child carries the original Kalshi ticker on `markets.ticker` after the Phase 0 reshape). A child without a corresponding terminal Kalshi market triggers `wait`.

### Coexistence with legacy resolver
The legacy `runKalshiResolve` driver and its per-child `dispatch`/`translateKalshiResolution` plumbing are untouched. The next Slice 1 task ("Wire resolver") swaps the driver over to `decideEventCommit + commitEvent`. The legacy path stays compilable through the transition.

### Files changed
- `src/lib/kalshi/resolve.ts`

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 45/45 vitest unit tests pass.
- `bunx oxlint src/lib/kalshi/resolve.ts` — 0 warnings, 0 errors.
- `bunx oxfmt --check src/lib/kalshi/resolve.ts` — clean (after `oxfmt --write`).
- `bun run lint` — 2 pre-existing warnings unchanged.

### Gotcha
- `(typeof kalshiResponse.markets)[number]` does not type-narrow through an inline `??[]` — the `Market[] | undefined` on `EventData.markets` resists indexing. Fix: bind `nestedKalshi = kalshiResponse.markets ?? []` first, then reference `(typeof nestedKalshi)[number]`.

## 2026-05-11 — Phase 0 Slice 1: Wire event-centric resolver

Replaced the market-centric resolver driver with an event-centric one. `runKalshiResolve` now selects from `events WHERE status='open' AND closesAt < now`, loads each event's child markets (`id`/`eventId`/`ticker`), groups by `events.sport` → Kalshi series, fans out per-event-ticker getEvent calls (4-way concurrency, basic-tier read budget), feeds each Kalshi response into `decideEventCommit`, and on a `commit` decision dispatches to a new `commitEvent` function in `data/events.ts`.

### `commitEvent` (Slice 1 stub) — `src/data/events.ts`
Takes `{eventId, perChild: ChildOutcome[]}`. Asserts the event is still open, iterates the children, and per-child dispatches to the legacy `resolveMarket`/`cancelMarket` data-layer calls:

- `resolved_yes` → `resolveMarket(id, "A")` — under the YES-first mapping established by `createEvent`, A is YES (`sharesA → reserveYes`), so the legacy single-side payout function does the right thing.
- `resolved_no` → `resolveMarket(id, "B")` — under YES-only trading no holder has `sharesB`, so all payouts compute to 0; a `SettlePayout amount=0` row gets emitted per holder, which is exactly what PRD §Settlement calls for.
- `cancelled_*` → `cancelMarket(id, "<reason>")`.

After all children are dispatched, flips `events.status = 'terminal'` so subsequent resolver passes skip the event. The single-DB-transaction body promised by PLAN §Phase 3 "rewire resolveMarket/cancelMarket into a single commitEvent" is intentionally deferred — that's the next plan task, which replaces the per-child dispatch with `computeSettlement` across all children in one tx.

### Summary structure (`KalshiResolveSummary`)
Reshaped to event-centric counters per PLAN §Phase 3 line 111:
- `considered` = number of past-close open events
- `committed` = events whose plan applied successfully
- `perChildOutcomes` = per-outcome-kind counts (`resolved_yes`/`resolved_no`/`cancelled_voided`/`cancelled_scalar`/`cancelled_no_settlement`)
- `skipped` = `{waited, kalshi_event_missing, commit_failed, no_event_ticker}`
- `commitFailures` and `childErrors` carry per-event / per-market failure reasons
- `totals.resolved` / `totals.cancelled` aggregates retained so `src/app/api/cron/resolve/route.ts` keeps gating its cache revalidation on them without changes.

### Files changed
- `src/lib/kalshi/resolve.ts` (driver rewrite; `decideEventCommit` untouched)
- `src/data/events.ts` (added `commitEvent`)

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 45/45 vitest unit tests pass.
- `bunx oxlint` on changed files — 0 warnings, 0 errors. `bun run lint` shows 2 pre-existing warnings unrelated to this change.
- `bunx oxfmt --check` on changed files — clean (after `oxfmt --write` on `events.ts`).

### Gotchas
- `data/events.ts` imports `cancelMarket`/`resolveMarket` from `data/markets` (runtime) and `ChildOutcome` from `lib/kalshi/resolve` (type-only via `import type`). `resolve.ts` imports `commitEvent` from `data/events` (runtime). At runtime this is a one-way edge — `events → markets` — because the type import is erased. TypeScript handles the type-only side of the cycle fine; left a comment in `events.ts` so future readers don't try to "fix" what isn't broken.
- `drizzle-orm.inArray` chokes on an empty list, so the event-id lookup is guarded by the early return when `eventRows.length === 0`.
- Cron route `route.ts` was left unchanged because the new summary keeps backward-compatible `totals.resolved`/`totals.cancelled` aggregates. Phase 3 may revisit the field shape; for Slice 1 the route's revalidation gate keeps working.

## 2026-05-11 — Phase 0 Slice 1: Adapt settlement + inline commitEvent

Folded per-child resolve/cancel work into a single transactional `commitEvent` driven by a new event-level settlement primitive. `commitEvent` no longer dispatches to `resolveMarket`/`cancelMarket` — it loads every child's pool + position rows in one pass, runs the pure decision function `computeEventSettlement`, and applies all balance credits, treasury moves, pool drains, market status flips, and ledger rows inside one `db.transaction`.

### New `computeEventSettlement` (`lib/settlement.ts`)
Additive — sits alongside the legacy per-child `computeSettlement` (still used by `resolveMarket`/`cancelMarket` for the integration test). Takes `{perChild: EventSettlementChildInput[]}` where each child carries its `outcome` (`resolved_yes`/`resolved_no`/`cancelled_*`), its `wpmReserve`, and a flat YES-only `positions[]` (`{userId, shares, costBasis}`). Returns per-child output bundles with `payouts`, `treasuryDelta`, `backstopAmount`, `finalStatus`, and `resolvedAs`.

Per-child semantics match PRD §Settlement:
- `resolved_yes`: holders with `shares > 0n` → `win` row of `amount = shares` (since YES shares pay 1 WPM each).
- `resolved_no`: holders with `shares > 0n` → `loss` row of `amount = 0n` (still emitted per PRD).
- `cancelled_*`: refund `costBasis` (or `zero` row if `costBasis = 0n`).

The function is pure — no DB I/O, no clock — so it'll slot into the Phase 3 settlement tests without further reshaping.

### Inlined `commitEvent` (`src/data/events.ts`)
- Opens one `db.transaction` for the whole event.
- Guards: `event_not_found` / `event_not_open` short-circuit.
- Loads `markets`/`amm_pools`/`positions` for the child set in parallel via `Promise.all`.
- Pre-flight pass classifies each child: already-resolved children get `{alreadyResolved: true}` without re-running settlement (idempotency under retry); already-cancelled likewise; missing market/pool emits an error result; remaining children feed into `settleablePlan`.
- Single call to `computeEventSettlement`.
- `applyChildSettlement` writes per-child: `balances` credits (only for `amount > 0`), `SettlePayout` rows (always, including loss/zero), `treasury` backstop or sweep with a `TreasuryBackstop` row when backstop fires, zeros `ammPools.wpmReserve`, flips `markets.status` to `resolved`/`cancelled`, and writes the `ResolveMarket`/`CancelMarket` transaction row.
- Final write flips `events.status='terminal'`.

### Legacy retention
- `resolveMarket`/`cancelMarket` in `data/markets.ts` are untouched — the integration test still calls them. `computeSettlement` (legacy single-child) is also untouched. Both go away in Phase 6 when the integration test is rewritten.
- `markets.resolvedOutcome` (A/B) is dual-written alongside `markets.resolvedAs` (yes/no) during the additive transition — A === yes under the YES-first mapping. Phase 1 drops the legacy column.

### Files changed
- `src/lib/settlement.ts` (added event-level API)
- `src/data/events.ts` (rewrote `commitEvent`, dropped imports of `cancelMarket`/`resolveMarket`)

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 45/45 vitest unit tests pass (no settlement tests broke; the new function will be exercised by Phase 3's `Settlement tests`).
- `bunx oxlint` on changed files — 0 warnings, 0 errors. `bun run lint` shows the same 2 pre-existing warnings.
- `bunx oxfmt --check` on changed files — clean.

### Gotchas
- `Promise.all([select, select, select])` inside a `db.transaction` is fine with `postgres-js` — drizzle serialises queries on the underlying transaction object, but the typescript signature returns a tuple so destructuring is ergonomic. The empty-childMarketIds branch returns `[[], [], []]` early because `inArray` chokes on an empty list (same gotcha as the previous resolver-driver task).
- `cancelReasonFor` had to retain a branch for `resolved_yes`/`resolved_no` to satisfy exhaustiveness — those paths are unreachable at runtime since they take the resolved branch, but the switch needs the cases. Returns `"oracle_cancel"` defensively.
- The `applyChildSettlement` walks payouts twice mentally (credit + ledger row), but a single loop suffices — the ledger row is emitted for every payout (including zero-amount loss/zero rows) per PRD §Settlement.

## 2026-05-11 — Phase 0 Slice 1: Drop `outcome` from `placeBet` action; delete `sellShares` action

Removed the legacy A/B `outcome` parameter from the `placeBet` server action's Zod schema and deleted `src/actions/sellShares.ts` outright. The action now passes the parsed `{marketId, amount}` straight through to the DAL — no key-picking required since the schema matches the DAL signature.

### Consumer cascade in `bet-controls.tsx`
The only consumer of either action was `src/components/bet-controls.tsx`, which had to be adjusted minimally to keep typecheck green ahead of the Phase 5 UI rewrite:
- Removed `import { sellShares } from "@/actions/sellShares"`.
- Changed `placeBet({ marketId, outcome, amount })` → `placeBet({ marketId, amount })`. The `outcome` state variable still drives display (which side is highlighted, which position is shown), but it no longer participates in the bet payload — the server always buys YES under the new model.
- Replaced `handleSell`'s body with an inline "Selling is disabled." error message so the surrounding sell-tab UI keeps compiling. The tab, related state (`sellShareAmount`, `tab`, `pickTab`), and presets are intentionally left in place — Phase 5 will remove the entire toggle/tab apparatus when it rewrites the bet form.

### Why a minimal UI patch instead of a full purge
The plan explicitly defers the A/B toggle and sell-tab removal to Phase 5 ("Bet controls / drawer: remove A/B toggle; single 'Buy YES' button per Market"). Excising the sell UI now would have rippled into `<Tabs>`/`<TabsList>`/`<TabsContent>` deletions and forced state cleanup beyond the scope of "delete the action file" — so the surgical replacement keeps the diff tight and lets the Phase 5 task own the structural redesign.

### Files changed
- `src/actions/placeBet.ts`
- `src/actions/sellShares.ts` (deleted)
- `src/components/bet-controls.tsx`

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 45/45 vitest unit tests pass.
- `bunx oxlint` on changed files — 0 warnings, 0 errors. `bun run lint` shows the same 2 pre-existing warnings unrelated to this change.
- `bunx oxfmt --check` on changed files — clean.

### Notes
- The `sellShares` DAL function in `src/data/trading.ts` is intentionally untouched — it's still called by `tests/integration/bet-and-resolve.test.ts` via `placeBetLegacy`'s sibling export, and Phase 6 rewrites that integration test. Same retention rationale as `placeBetLegacy` and `createMarket`.
- The `SellShares` enum value in `transactionTypes` similarly stays for now — its drop is tied to the trading.ts DAL purge in Phase 4.

## 2026-05-11 — Phase 0 Slice 1: End-to-end smoke test

Added `tests/integration/event-flow.test.ts` exercising the full Slice 1 flow against a real Postgres database:

1. Seeds treasury + two users with starting balances.
2. Runs `binary-healthy.json` through `translateKalshiEvent` to produce the new `{event, markets[]}` shape.
3. Calls `createEvent(translation.value)` and asserts both child Markets + pools land in one transaction.
4. Bumps the child `closesAt` past `Date.now()` (fixture timestamp is in the past relative to live `placeBet`'s "betting has closed" check) and places a YES bet via the production `placeBet` on the first child.
5. Force-commits the Event with `commitEvent({eventId, perChild: [{resolved_yes}, {resolved_no}]})`.
6. Verifies: `events.status='terminal'`, both `markets.status='resolved'` with correct `resolvedAs`, winner's balance credited by exactly `shares` WPM, loser balance untouched, `SettlePayout` row emitted for the winner with `kind='win'` and `amount=shares`, one `ResolveMarket` row per child, both pools drained, and treasury grew.

### Files changed
- `tests/integration/event-flow.test.ts` (new)

### Verification
- `bun run test:integration` — both `event-flow.test.ts` and the legacy `bet-and-resolve.test.ts` pass (2/2 tests).
- `bun run test` — 45/45 unit tests still pass.
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bunx oxlint tests/integration/event-flow.test.ts` — 0 warnings, 0 errors.
- `bunx oxfmt --check tests/integration/event-flow.test.ts` — clean.

### Gotchas
- The translator hardcodes the Event's `closesAt` to the fixture's `expected_expiration_time` (2026-04-25). That timestamp is in the past relative to "today" (2026-05-11), so the production `placeBet` would reject any bet against the just-created markets. The test bumps the child Markets' `closesAt` forward one hour after `createEvent` returns, before driving the bet. `commitEvent` does not consult `closesAt`, so no further patching is needed for the commit step.
- `TRUNCATE TABLE` list extended to include `events` (the existing `bet-and-resolve.test.ts` omits it; harmless there because that test never inserts events). CASCADE handles ordering automatically.
- Mocks for `@/data/auth` and `next/cache` reuse the same pattern as `bet-and-resolve.test.ts`. `vi.mock` calls are hoisted; runtime imports use top-level `await import(...)` to defer module eval until after the mocks register.

## 2026-05-11 — Phase 0 Slice 2: Author multi-outcome fixture

Added `src/lib/kalshi/fixtures/multi-outcome-healthy.json` — a 5-Market Kalshi Event modelled as the 2026 NBA Championship winner (5 candidate teams). All five child Markets share `expected_expiration_time = 2026-06-21T03:00:00Z` so the upcoming `inconsistent_close_times` gate has nothing to flag on the happy path.

Quote shapes are calibrated so the translator's per-Market checks all pass:
- Each child's `yes_ask - yes_bid = 0.02`, well under `MAX_SPREAD = 0.1`.
- Midpoints sum to ~1.0 across the field (0.30 + 0.25 + 0.20 + 0.15 + 0.10), modelling a coherent multi-outcome distribution where exactly one candidate wins. Translator does not enforce this sum — it's just realistic — so future fixtures can be skewed without breaking gates.

This fixture will drive the upcoming Slice 2 translator path (iterate `event.markets`, build N rows) and ingest test (one `events` row + 5 `markets`/`ammPools` rows).

### Files changed
- `src/lib/kalshi/fixtures/multi-outcome-healthy.json` (new)

### Verification
- `bunx oxfmt --check` on the new fixture — clean.
- JSON parses cleanly; spreads = 0.02 each, midpoints sum to 1.0.
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 45/45 vitest unit tests pass (no change; fixture not yet consumed).

## 2026-05-11 — Phase 0 Slice 2: Multi-outcome ingest smoke test

Added an integration test that exercises the multi-outcome ingest path end-to-end against a real Postgres database, using the 5-Market `multi-outcome-healthy.json` fixture as the driver.

Test flow:
1. Seed treasury.
2. Translate the fixture through `translateKalshiEvent` and assert the `ok` translation has 5 child Markets.
3. Call `createEvent(translation.value)` and verify it returns `{created: true}`.
4. Assert one `events` row exists with `sport='nba'`, `status='open'`.
5. Assert five `markets` rows under that event, all `status='open'`.
6. For each child market, load its `amm_pools` row and assert that
   `priceYes = reserveNo / (reserveYes + reserveNo)` matches the translator's
   per-child midpoint within `toBeCloseTo(_, 3)` rounding tolerance.

Added as a second `describe` block in `tests/integration/event-flow.test.ts` to share the existing mocks, db client setup, and TRUNCATE helpers with the Slice 1 smoke test.

### Why this satisfies Slice 2

The other three Slice 2 line items (translator N-Market path, all-or-nothing rejection, single-tx ingest persistence) were already implemented during Slice 1 — see prior progress entries for the translator rewrite and `createEvent`. This smoke test is the only outstanding deliverable that exercises those changes on N>2 children against a real DB, which is what the plan called for.

### Verification probability assertion

`initializePool` derives reserves from `initialProbabilityYes` as:
- `probBps = round(probYes * 10000)`, `total = 2 * seed`
- `sharesA = total * (10000 - probBps) / 10000` → `reserveYes`
- `sharesB = total - sharesA` → `reserveNo`

So `priceYes = reserveNo / (reserveYes + reserveNo) ≈ probYes`. The 3-decimal tolerance absorbs the bps rounding (each bps is 0.0001).

The pool read uses `pool.reserveYes ?? pool.reserveA` (and equivalent for No) to be resilient to the Slice 1 additive transition where both legacy and new reserve columns coexist — in `createEvent` the new columns are populated, but the fallback keeps the test robust if that ever flips.

### Files changed
- `tests/integration/event-flow.test.ts` (added second `describe` block)

### Verification
- `bun run test:integration` — 3/3 tests pass (Slice 1 + Slice 2 smoke + legacy `bet-and-resolve`).
- `bun run test` — 45/45 unit tests still pass.
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bunx oxlint tests/integration/event-flow.test.ts` — 0 warnings, 0 errors.
- `bunx oxfmt --check tests/integration/event-flow.test.ts` — clean (after `oxfmt --write`).

## 2026-05-11 — Phase 1: Drop `SellShares` (dead-code purge)

Removed all references to `SellShares` from the codebase. The `sellShares` data-layer function in `src/data/trading.ts` had zero remaining callers (verified by grep for `sellShares(`) — Phase 0 Slice 1 already deleted the `actions/sellShares.ts` server action and `bet-controls.tsx` handler, so the DAL function was already orphaned. Closing it out is a pure deletion with no behavioural cascade.

### Three plan bullets closed by this change
- Phase 1 Schema migration: "Drop `SellShares` from `transactionTypes`" — removed from the enum array in `src/lib/db/schema/app.ts`. No DB migration needed: drizzle's `text("type", { enum: [...] })` is TypeScript-only on text columns (no Postgres-side enum exists).
- Phase 1 Type purges: `SellShares` variant in the `Transaction` union (`src/lib/types.ts`) deleted.
- Phase 4 Data layer: `sellShares` function + `SellSharesInput`/`SellSharesResult` types deleted from `src/data/trading.ts`. With the function gone, `calculateSell` is now unimported there; cleaned up the `@/lib/amm` import to drop it (the function itself stays in `lib/amm.ts` for now — its deletion is a separate Phase 1 AMM rewrite bullet).
- Also cleaned up an unused `and` import from drizzle-orm in `trading.ts` (the function that used it was the deleted `sellShares`).

### Phase 0 Slice 2 admin
Marked three Slice 2 plan bullets complete that were verified done by prior Slice 1 work but never struck through: translator iterates `event.markets`, all-or-nothing rejection with per-child reasons, ingest driver persists one Event + N markets/pools atomically. Confirmed by reading the current `translator.ts`, `ingest.ts`, and `createEvent` in `data/events.ts`.

### Files changed
- `src/data/trading.ts` (delete sellShares fn + types; clean up imports)
- `src/lib/types.ts` (delete SellShares variant from Transaction union)
- `src/lib/db/schema/app.ts` (delete "SellShares" from transactionTypes)
- `PLAN.md` (mark four bullets complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 45/45 vitest unit tests pass.
- `bun run test:integration` — 3/3 tests pass.
- `bunx oxlint` on changed files — 0 warnings, 0 errors. `bun run lint` shows the same 2 pre-existing warnings.
- `bunx oxfmt --check` on changed files — clean.

### Notes
- The DAL function had no DB-migration concern because the `transactions.type` column is plain TEXT with a TS-side enum hint, not a Postgres ENUM type. Historical rows with `type='SellShares'` (if any existed) would still load and serialize as raw text, but in practice there are none — the function never ran in production.
- `calculateSell` and `isqrt` in `lib/amm.ts` are now both fully orphaned, but their deletion is scheduled as a separate Phase 1 AMM rewrite bullet ("Delete `calculateSell` and `isqrt`") and is not part of this change.

## 2026-05-11 — Phase 1: Delete `calculateSell` and `isqrt` from AMM

Removed the two orphaned legacy AMM functions from `src/lib/amm.ts` and their corresponding test cases from `src/lib/amm.test.ts`. Both functions had zero production consumers — `calculateSell` was last referenced by `data/trading.ts:sellShares`, which was deleted in the prior "Drop SellShares" task, and `isqrt` was a private dependency of `calculateSell`.

Closes two plan bullets:
- Phase 1 AMM rewrite: "Delete `calculateSell` and `isqrt`. Remove their exports from any consumer."
- Phase 3 AMM tests: "Remove `calculateSell` and `isqrt` test cases (their absence is the type contract)."

### Files changed
- `src/lib/amm.ts` (delete `calculateSell` and `isqrt`)
- `src/lib/amm.test.ts` (delete describe blocks for both)
- `PLAN.md` (mark two bullets complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 37/37 unit tests pass (was 45 before; 8 deleted: 4 isqrt + 4 calculateSell).
- `bunx oxlint` on changed files — 0 warnings, 0 errors.
- `bunx oxfmt` on changed files — clean (after `oxfmt --write` on `amm.ts`).

## 2026-05-11 — Phase 2: Delete `translateKalshiResolution` from translator.ts

Removed the legacy per-child resolution decision function from `src/lib/kalshi/translator.ts`. The function had no production callers — the resolver driver was rewired to `decideEventCommit` (in `src/lib/kalshi/resolve.ts`) during Phase 0 Slice 1, and the only remaining references were its own unit tests.

### What got deleted from `translator.ts`
- `translateKalshiResolution(event)` function
- `ResolutionTranslation` discriminated-union type (7 variants)
- `TERMINAL_KALSHI_STATUSES` const and `isTerminalKalshiStatus` helper — `resolve.ts` already carries its own private copy of `TERMINAL_KALSHI_STATUSES` for the new `decideEventCommit` path, so deleting these from the translator does not orphan the live resolver

### What got deleted from `translator.test.ts`
- Entire `describe("translateKalshiResolution", ...)` block (7 cases)
- Unused fixture imports: `not-settled-yet`, `settled-a-wins`, `settled-ambiguous`, `settled-b-wins`, `settled-scalar`, `settled-voided`
- Stale named export from the `./translator.js` import

### Fixture retention
Left the `settled-*.json` and `not-settled-yet.json` Kalshi fixtures in `src/lib/kalshi/fixtures/` even though they are now orphaned by the translator tests. The Phase 3 "Resolver decision tests" block (PLAN §Phase 3 lines 113-119) will need terminal-Kalshi-market fixtures to exercise `decideEventCommit`; these existing settle-* shapes will likely be reusable then. Plan §Phase 6 only calls out `non-binary.json` and `pair-inconsistent.json` for explicit deletion.

### Files changed
- `src/lib/kalshi/translator.ts` (delete `translateKalshiResolution`, `ResolutionTranslation`, terminal-status helpers)
- `src/lib/kalshi/translator.test.ts` (delete `describe("translateKalshiResolution"...)` block + orphaned imports)
- `PLAN.md` (mark one bullet complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 30/30 unit tests pass (was 37; 7 deleted with the `translateKalshiResolution` describe block).
- `bun run test:integration` — 3/3 tests pass.
- `bunx oxlint` on changed files — 0 warnings, 0 errors.
- `bunx oxfmt --check` on changed files — clean (after `oxfmt --write` on `translator.test.ts`).

## 2026-05-11 — Phase 2: Add `too_many_markets` translator variant

Added a hard cap on child Markets per Event in the translator. Events with >30 children are now rejected wholesale via a new `too_many_markets` discriminated-union variant, before any other validation runs.

Changes in `src/lib/kalshi/translator.ts`:
- Added `MAX_MARKETS_PER_EVENT = 30` constant, sourced from PRD §Further Notes (also documented as an assumption at the top of `PLAN.md`).
- New `TranslationResult` variant: `{ kind: 'too_many_markets', eventTicker, count }`. Carries both the event ticker (for logging consistency with sibling variants) and the actual market count (so operators can see how badly the cap was breached).
- Check runs first in `translateKalshiEvent`, before close-time parsing and per-Market quote/spread checks. Failing fast keeps oversized payloads out of the heavier downstream validation.

Changes in `src/lib/kalshi/ingest.ts`:
- `SkipReasonKind` automatically picks up the new variant via its `Exclude<TranslationResult["kind"], "ok">` definition, but `emptySkipCounters()` had to be updated to initialise the new `too_many_markets: 0` field — `Record<SkipReasonKind, number>` enforces exhaustiveness.

### Test fixture choice
Instead of authoring a 31-market JSON fixture, the new test case constructs the oversized event in-memory by cloning a child from `binary-healthy.json` 31 times. Two reasons:
1. The JSON would be ~31× redundant — every child carries the same shape, only the ticker varies.
2. A real 31-Market Kalshi Event doesn't exist for any current series; an in-memory construction is more honest about the test's purpose (cap enforcement) than a fabricated fixture.

The clone uses `Array.from({ length: 31 }, (_, i) => ({ ...child, ticker: `...-${i}` }))` so each child has a distinct ticker (translator does not currently dedupe, but giving each child a unique ticker matches the production shape).

### Files changed
- `src/lib/kalshi/translator.ts` (added constant, new variant, check)
- `src/lib/kalshi/ingest.ts` (extended `emptySkipCounters`)
- `src/lib/kalshi/translator.test.ts` (new test case)
- `PLAN.md` (marked bullet complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 31/31 unit tests pass (was 30; +1 new translator case).
- `bunx oxlint` on changed files — 0 warnings, 0 errors.
- `bunx oxfmt --check` on changed files — clean.

## 2026-05-11 — Phase 2: Add `inconsistent_close_times` translator variant

Added a new translator reject variant that fires when sibling child Markets carry mismatched `expected_expiration_time` values. The Event-level `closesAt` is still sourced from the first child; any later child whose timestamp differs is collected as an "offender" and the translation rejects the whole Event.

Changes in `src/lib/kalshi/translator.ts`:
- New `TranslationResult` variant: `{ kind: 'inconsistent_close_times', eventTicker, expected, offenders }`. `offenders` is `{ ticker, raw }[]` so operators can see which children disagreed and with what raw value.
- Check runs after `unparseable_close_time` (so we know `rawCloseTime` is well-formed) and before per-Market quote/spread checks (rejecting cheaply before walking the heavier validation).
- `raw: m.expected_expiration_time ?? ""` — the SDK type allows `string | null | undefined`, so coalesce to empty-string for the variant's `raw: string` field.

Changes in `src/lib/kalshi/ingest.ts`:
- Added `inconsistent_close_times: 0` to `emptySkipCounters()`. The `SkipReasonKind` union picks the new variant up automatically via its `Exclude<TranslationResult["kind"], "ok">` derivation.

### Test
Added a translator test case that clones the binary-healthy fixture but rewrites the second child's `expected_expiration_time` to a different ISO timestamp, then asserts `kind='inconsistent_close_times'` with the expected `eventTicker`/`expected`/`offenders` fields. Constructed in-memory rather than as a JSON fixture for the same reason as the `too_many_markets` test — the JSON would be near-duplicate of `binary-healthy.json` modulo one field.

### Files changed
- `src/lib/kalshi/translator.ts`
- `src/lib/kalshi/ingest.ts`
- `src/lib/kalshi/translator.test.ts`
- `PLAN.md`

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 32/32 unit tests pass (was 31; +1 new translator case).
- `bunx oxlint` on changed files — 0 warnings, 0 errors.
- `bunx oxfmt --check` on changed files — clean (after `oxfmt --write` on `translator.test.ts`).

## 2026-05-11 — Phase 2: Add 5-Market translator happy-path test

Added the multi-outcome happy-path translator test that the plan called for. Drives `translateKalshiEvent` with the existing `multi-outcome-healthy.json` fixture (5-Market NBA Championship event) and asserts:
- One `event` row with the correct id, sport, name, and `closesAt` parsed from the shared `expected_expiration_time`.
- 5 child `markets` rows in fixture order, each carrying the right `id`/`name`/`ticker`/`closesAt`/`seedAmount`.
- Each child's `initialProbabilityYes` matches its own bid/ask midpoint (0.30, 0.25, 0.20, 0.15, 0.10).

The midpoints sum to 1.0 across the field — coherent multi-outcome distribution — but the translator doesn't enforce that, so the assertion is just per-child equality. No production-code change needed; the translator already handled N>2 children after the Phase 0 Slice 2 work.

### Files changed
- `src/lib/kalshi/translator.test.ts` (new test case + multi-outcome-healthy fixture import)
- `PLAN.md` (marked bullet complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 33/33 unit tests pass (was 32; +1 new translator case).
- `bunx oxlint src/lib/kalshi/translator.test.ts` — 0 warnings, 0 errors.
- `bunx oxfmt --check` — clean (after `oxfmt --write` re-flowed the inline expected-array literal).

## 2026-05-11 — Phase 2: Event-level ingest summary counters

Split the ingest driver's single `created` counter into two distinct counters per the PRD's Event/Market vocabulary: `createdEvents` (number of `events` rows written) and `createdMarkets` (sum of child Markets across those events). Both surface in `SeriesIngestSummary` and the top-level `totals` rollup.

The Markets count is derived from `result.value.markets.length` for each successful translation — `createEvent` doesn't need a richer return shape because the translator output is already in hand at the call site.

### Cron route revalidation gate
`src/app/api/cron/ingest/route.ts` was gated on `summary.totals.created > 0`. Now gates on `summary.totals.createdEvents > 0` — semantically the same boolean (every created Event implies at least one Market) but matches the new naming. Could equivalently gate on `createdMarkets`; picked `createdEvents` because the homepage groups by Event and the revalidation is for the markets-list view that walks Event rows.

### Files changed
- `src/lib/kalshi/ingest.ts`
- `src/app/api/cron/ingest/route.ts`
- `PLAN.md` (mark Phase 2 line 92 complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts` overload error remains.
- `bun run test` — 33/33 unit tests pass.
- `bunx oxlint` on changed files — 0 warnings, 0 errors.
- `bunx oxfmt --check` on changed files — clean.

## 2026-05-11 — Phase 3: Void semantics in `decideEventCommit`

Extended the resolver decision core's happy path with Event-level void semantics
(PRD §Resolver). When every child Market in a terminal Event has Kalshi
`result: "no"`, the decision now rewrites every child's outcome from
`resolved_no` to `cancelled_voided` so each holder is refunded their cost basis
rather than booking a loss. Single post-pass over the assembled `perChild`
array — no change to the per-child walk or the wait/scalar/unknown branches.

`commitEvent`/`computeEventSettlement` already handle `cancelled_voided` as a
refund-on-costBasis branch (added in Phase 0 Slice 1), so no downstream wiring
needed.

### New test file: `src/lib/kalshi/resolve.test.ts`

Drives `decideEventCommit` with the existing `settled-voided.json` fixture
(2-Market rainout where both children settle "no"). Builds a minimal
`WampumEventForDecision` whose child tickers/ids mirror the Kalshi event, then
asserts the decision is `commit` with both children carrying
`outcome: 'cancelled_voided'`.

### Vitest config: `server-only` and `@/` aliases for unit tests

`resolve.ts` imports `"server-only"` at the top and uses `@/` path aliases.
The integration vitest config already had these aliased; the unit config did
not, so the new resolve test failed on module load. Added both aliases to
`vitest.config.ts` (mirroring the integration setup — same `server-only` stub
file under `tests/integration/stubs/`). With the alias in place, any future
unit tests that need to import server-only modules just work.

### Files changed
- `src/lib/kalshi/resolve.ts` (void-semantics post-pass)
- `src/lib/kalshi/resolve.test.ts` (new)
- `vitest.config.ts` (server-only + @ aliases)
- `PLAN.md` (mark two bullets complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts`
  overload error remains.
- `bun run test` — 34/34 unit tests pass (was 33; +1 new resolve test).
- `bunx oxlint` on changed files — 0 warnings, 0 errors.
- `bunx oxfmt --check` on changed files — clean (after `oxfmt --write` on the
  new test file).

### Notes
- The decision is structured so void semantics only triggers when every child
  resolved to `no` — siblings that hit `cancelled_scalar` or `resolved_yes`
  keep their outcomes and the Event commits normally. The `perChild.length > 0`
  guard is defensive against an Event with zero children (shouldn't happen in
  practice — the translator rejects those via `unparseable_close_time`).
- Deadline degradation (`cancelled_no_settlement` past the 48h deadline) is
  the next Phase 3 bullet; it's a separate code path that runs before the
  happy-path walk (since it gates on `now` vs `closesAt + 48h`).

## 2026-05-11 — Phase 3: Deadline degradation in `decideEventCommit`

Implemented the 48h Settlement Deadline degradation branch in
`decideEventCommit`. When `now >= closesAt + 48h` and any child Market is
non-terminal on Kalshi's side, that child is now committed as
`cancelled_no_settlement` (cost-basis refund) so the Event commits rather than
blocking indefinitely. Cleanly-settled siblings still resolve on their actual
`result` in the same commit plan.

### Implementation notes
- Added `SETTLEMENT_DEADLINE_MS = 48 * 60 * 60 * 1000` module-level constant
  (PRD §Further Notes calls it out as baked-in for v1).
- `deadlineReached = now >= wampumEvent.closesAt + SETTLEMENT_DEADLINE_MS` is
  computed once at the top of the function. The per-child loop branches on it:
  - non-terminal child + deadlineReached → push `cancelled_no_settlement`.
  - non-terminal child + before deadline → return `{kind: 'wait'}` (preserves
    pre-deadline behaviour).
  - terminal child + unrecognised result + deadlineReached → also degrade to
    `cancelled_no_settlement` so a malformed terminal payload past the
    deadline doesn't stall the Event. This is a small policy extension beyond
    the strict "non-terminal" reading of the PRD, but matches the deadline's
    intent: bound the worst-case duration of a stuck position.
  - terminal child + unrecognised result + before deadline → still returns
    `wait` so operators have a chance to inspect the malformed payload.
- Renamed the `_now` parameter to `now` now that the function reads it.
- Void semantics check (`every === resolved_no`) is unchanged — it sits after
  the per-child walk, so a commit plan containing any
  `cancelled_no_settlement` rows can never trigger void (the "all candidates
  lost" reading requires complete information, which a deadline-cancelled
  child does not provide).

### Tests added (`src/lib/kalshi/resolve.test.ts`)
- `waits when any child is non-terminal and the deadline has not been reached`
  — drives the existing `not-settled-yet.json` fixture (both children
  `status: 'active'`) at `closesAt + 1h` and asserts `wait`.
- `degrades unsettled children to cancelled_no_settlement past the deadline
  alongside cleanly-settled siblings` — constructs a mixed Kalshi payload
  in-memory by combining one settled child (from `settled-a-wins.json`) and
  one active child (from `not-settled-yet.json`), driven at `closesAt + 49h`.
  Asserts the commit plan carries both `resolved_yes` and
  `cancelled_no_settlement` per child.
- Pre-existing `voids the event when every child settled no` test updated to
  use the same `CLOSES_AT`/`BEFORE_DEADLINE` constants for consistency; no
  behavioural change.

### Why an in-memory mixed payload, not a fixture
The existing `not-settled-yet.json` and `settled-a-wins.json` cover the
single-state cases; constructing the mixed payload from them avoids a
near-duplicate JSON file and keeps the test's intent (mixed state) visible at
the call site. Same rationale as the in-memory test construction used for
`too_many_markets` and `inconsistent_close_times`.

### Plan bullets closed
- Phase 3 Resolver decision core: "Deadline degradation: `now >= closesAt +
  48h` AND any non-terminal child → unsettled children get
  `cancelled_no_settlement`, cleanly-settled siblings resolve on their
  `result`."
- Phase 3 Resolver decision tests: "`wait` when any child non-terminal and
  deadline not reached."
- Phase 3 Resolver decision tests: "`commit` with mixed
  `cancelled_no_settlement` + `resolved_*` past the deadline."

### Files changed
- `src/lib/kalshi/resolve.ts`
- `src/lib/kalshi/resolve.test.ts`
- `PLAN.md`

### Verification
- `bunx tsc --noEmit` — only the pre-existing
  `vitest.integration.config.ts` overload error remains.
- `bun run test` — 36/36 unit tests pass (was 34; +2 new resolve tests).
- `bunx oxlint` on changed files — 0 warnings, 0 errors.
- `bunx oxfmt --check` on changed files — clean.

## 2026-05-11 — Phase 3: Decision test for clean per-child resolutions

Added the missing `decideEventCommit` test case for the basic happy path where
every child Market settles cleanly to `yes` or `no` (no void, no degradation).
Drives the existing `settled-a-wins.json` fixture (2-Market NYY-vs-BOS event,
NYY child settled `yes`, BOS child settled `no`) and asserts the commit plan
emits one `resolved_yes` and one `resolved_no` per child in fixture order.

Sits as the first test in `describe("decideEventCommit", ...)` since it's the
canonical happy path; void/wait/deadline cases follow.

### Files changed
- `src/lib/kalshi/resolve.test.ts`
- `PLAN.md`

### Verification
- `bun run test` — 37/37 unit tests pass (was 36; +1 new resolve test).
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts`
  overload error remains.
- `bunx oxlint src/lib/kalshi/resolve.test.ts` — 0 warnings, 0 errors.
- `bunx oxfmt --check src/lib/kalshi/resolve.test.ts` — clean.

## 2026-05-11 — Phase 3: Decision test for cancelled_scalar with normally-resolved siblings

Added the missing `decideEventCommit` test case for a mixed commit where one
child settles `scalar` (refund-on-costBasis) alongside a sibling that resolves
normally on its Kalshi `result`. Constructs the payload in-memory by combining
the first child from `settled-a-wins.json` (yes) with the first child from
`settled-scalar.json` (scalar), then asserts the commit plan emits
`[resolved_yes, cancelled_scalar]`.

Same in-memory mix pattern as the deadline-degradation test — keeps the test's
intent (mixed scalar/clean state) visible at the call site without fabricating
a near-duplicate JSON fixture.

### Files changed
- `src/lib/kalshi/resolve.test.ts` (new test case + `settled-scalar.json`
  fixture import)
- `PLAN.md` (mark Phase 3 resolver decision test bullet complete)

### Verification
- `bun run test` — 38/38 unit tests pass (was 37; +1 new resolve test).
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts`
  overload error remains.
- `bunx oxlint src/lib/kalshi/resolve.test.ts` — 0 warnings, 0 errors.
- `bunx oxfmt --check src/lib/kalshi/resolve.test.ts` — clean.

## 2026-05-11 — Phase 1: Drop `ix_markets_status_closes` index

Removed the legacy composite index from the `markets` table now that the
resolver driver selects exclusively from `events` via
`ix_events_status_closes` (added in Phase 0). No production query reads
`markets.status` or `markets.closesAt` for filtering — only single-row
lookups by primary key — so the index gives nothing back at this point.

`markets.status` and `markets.closesAt` columns themselves stay for now:
they're still used by the legacy `createMarket`/`resolveMarket`/
`cancelMarket` paths kept around for `tests/integration/bet-and-resolve.test.ts`.
Their drop is tied to the broader Phase 1 column-purge bullet (line 41).

### Migration regen
Followed the same procedure as the prior regen:
1. Deleted `0000_petite_grey_gargoyle.sql` and `meta/0000_snapshot.json`.
2. Stubbed `meta/_journal.json` with empty `entries: []` so drizzle-kit
   could compute prevId/idx for a fresh emit.
3. Ran `bun run db:generate` — produced `0000_mute_gressill.sql` plus
   updated snapshot + journal. Treasury seed re-appended by the wrapper.
4. Verified `ix_markets_status_closes` is absent from the new SQL
   (`grep -c` returned 0).
5. `oxfmt --write` on the regenerated snapshot/journal to normalise
   trailing newlines.

### Files changed
- `src/lib/db/schema/app.ts` (drop index, inline the `markets` pgTable
  callback now that it has no second argument)
- `src/lib/db/migrations/0000_petite_grey_gargoyle.sql` (deleted)
- `src/lib/db/migrations/0000_mute_gressill.sql` (new)
- `src/lib/db/migrations/meta/0000_snapshot.json` (regenerated)
- `src/lib/db/migrations/meta/_journal.json` (regenerated)
- `PLAN.md` (mark Phase 1 line 45 complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts`
  overload error remains.
- `bun run test` — 38/38 unit tests pass.
- `bun run test:integration` — 3/3 tests pass (migrations apply cleanly,
  no broken FKs or index drops in flight).
- `bunx oxlint src/lib/db/schema/app.ts` — 0 warnings, 0 errors.
- `bunx oxfmt --check` on changed files — clean (after `oxfmt --write`
  on snapshot + journal).

## 2026-05-11 — Phase 3: Decision test for 5-child mixed commit

Added the final outstanding `decideEventCommit` resolver decision test:
a 5-child Event past the 48h deadline that combines three cleanly-resolved
children (`resolved_yes` / `resolved_no` / `resolved_yes`), one
`cancelled_scalar`, and one `cancelled_no_settlement` (still-active child
past deadline). Asserts the commit plan emits all five outcomes in order.

Constructed in-memory by combining fixture children from `settled-a-wins`,
`settled-scalar`, and `not-settled-yet`. The third clean child is a clone
of the yes-settled NYY market with `-CLONE` appended to its ticker so the
wampum-side ticker match remains unambiguous.

The yes/no/yes mix on the cleanly-settled trio is intentional — three
`resolved_no` children alongside a scalar + pending would NOT trip the
void-semantics post-pass (since `every === resolved_no` requires every
outcome be `resolved_no`, and the pending child commits as
`cancelled_no_settlement`), but using a mix keeps the test's intent visible
at the call site: "this is a heterogeneous commit plan, not a void event".

### Files changed
- `src/lib/kalshi/resolve.test.ts` (new test case)
- `PLAN.md` (mark Phase 3 line 119 complete)

### Verification
- `bun run test` — 39/39 unit tests pass (was 38; +1 new resolve test).
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts`
  overload error remains.
- `bunx oxlint src/lib/kalshi/resolve.test.ts` — 0 warnings, 0 errors.
- `bunx oxfmt --check src/lib/kalshi/resolve.test.ts` — clean.

### Notes
- This closes the Phase 3 "Resolver decision tests" block entirely; the
  remaining Phase 3 work is the resolver driver bullets and Settlement
  tests (settlement.test.ts updates and event-atomic commit assertions).

## 2026-05-11 — Phase 2: Translator fixture cleanup

Removed the now-unused legacy fixtures `non-binary.json` and
`pair-inconsistent.json` from `src/lib/kalshi/fixtures/`. Both fixtures
were holdovers from the binary/A-B model and no longer referenced by any
source code — confirmed via grep across `src/` and `tests/` (only PRDs
and PLAN.md mentioned them). The `non_binary` and `pair_inconsistent`
discriminator variants were already deleted from `translator.ts` in
earlier Phase 0 work; removing the fixtures closes the loop.

Added `multi-outcome-wide-spread.json` — a 5-child NBA championship
fixture where one child (OKC) carries a wide bid-ask spread while the
other four are within `MAX_SPREAD`. Added a paired test case verifying
that the per-child spread gate calls out *exactly* the offending child's
ticker in `reasons`, not the whole event. This complements the existing
2-Market wide-spread test by exercising the all-or-nothing rejection
shape under the multi-outcome model where most children are healthy.

### Files changed
- `src/lib/kalshi/fixtures/non-binary.json` (deleted)
- `src/lib/kalshi/fixtures/pair-inconsistent.json` (deleted)
- `src/lib/kalshi/fixtures/multi-outcome-wide-spread.json` (new)
- `src/lib/kalshi/translator.test.ts` (import + new test case)
- `PLAN.md` (mark Phase 2 fixture-cleanup bullet complete)

### Verification
- `bun run test` — 40/40 unit tests pass (was 39; +1 new translator test).
- `bunx tsc --noEmit` — only the pre-existing
  `vitest.integration.config.ts` overload error remains.
- `bunx oxlint` on changed files — 0 warnings, 0 errors.
- `bunx oxfmt --check` on changed files — clean (after `oxfmt` rewrap of
  the new test case to a single line).

### Notes
- The new test asserts `result.reasons` *exactly equals*
  `[{ ticker: "KXNBACHAMP-27-OKC", reason: "spread_too_wide" }]` rather
  than using `some(...)`. The stronger contract guards against a future
  bug where the translator would over-report reasons (e.g. flag healthy
  siblings as wide-spread) — that would silently pass `some(...)` but
  break the explicit array equality.

## 2026-05-11 — Phase 1: Rename AMM pool symbols to YES/NO

Renamed `AMMPool.sharesA/sharesB` → `reserveYes/reserveNo` and
`initializePool`'s `initialProbabilityA` parameter → `initialProbabilityYes`.
This is the internal-rename half of the AMM rewrite — function signatures
(calculateBuy still takes an `"A" | "B"` outcome, calculatePrices still
returns `priceA/priceB`) stay stable so UI consumers don't break. The
remaining AMM-rewrite items (drop the outcome param, switch return shapes
to priceYes/priceNo) are still pending and need a coordinated UI sweep.

Mapping rationale: under the YES-first model A === YES. `sharesA` was the
side that *decreases* when A is bought, which corresponds to the side that
decreases when YES is bought — i.e. `reserveYes`. So `sharesA → reserveYes`
and `sharesB → reserveNo`. `priceA` is calculated as `reserveNo / total`,
which is the YES price under the same mapping (consumer return shape
unchanged for now).

The legacy positions table still uses `sharesA/sharesB` columns — those
references in `data/trading.ts`, `data/positions.ts`, and
`lib/settlement.ts` are positions data, not pool data, and are out of
scope for this task.

### Files changed
- `src/lib/amm.ts` (rename properties and local vars; rename
  initializePool parameter)
- `src/lib/types.ts` (AMMPool type now `reserveYes/reserveNo`)
- `src/lib/amm.test.ts` (assertions on pool.reserveYes/.reserveNo;
  invariant checks)
- `src/data/trading.ts` (placeBet and placeBetLegacy now construct/read
  AMMPool with reserveYes/reserveNo; dual-write to DB unchanged)
- `src/data/events.ts` (createEvent reads pool.reserveYes/.reserveNo when
  writing the ammPools row; drop the now-stale "Slice 1 additive" comment)
- `src/data/markets.ts` (toPool() and legacy createMarket() use
  reserveYes/reserveNo)
- `PLAN.md` (mark Phase 1 line 57 complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing
  `vitest.integration.config.ts` overload error remains.
- `bun run test` — 40/40 unit tests pass.
- `bun run test:integration` — 3/3 tests pass (migrations still apply
  cleanly, placeBetLegacy + bet-and-resolve flow still works through the
  renamed AMM).
- `bunx oxlint` on changed files — 0 warnings, 0 errors.
- `bunx oxfmt --check` on changed files — clean (after `oxfmt` rewrap of
  the ternary in calculateBuy onto one line).

### Notes
- The `initialProbabilityA` field on the `Transaction` CreateMarket variant
  in `lib/types.ts` was NOT renamed — that type is dead code (no consumer
  imports `Transaction`), and the corresponding `legacyCreateMarket`
  consumer in `data/markets.ts` and `tests/integration/bet-and-resolve.test.ts`
  still pass `initialProbabilityA` as a payload key. Those callers get
  cleaned up alongside the legacy `createMarket` removal in Phase 4.
- This unblocks the rest of Phase 1's AMM-rewrite tasks (drop outcome
  param, rename calculatePrices/calculateOdds return keys) since the pool
  data structure now matches the YES-only AMM contract.

## 2026-05-11 — Phase 1: Drop `outcome` param from `calculateBuy`

Rewrote `calculateBuy` to a YES-only signature `(pool, amount)`, dropping the
`"A" | "B"` outcome param per ADR-0007. The function now always treats
`reserveNo` as the deposited side (grows by `amount`) and `reserveYes` as the
retained side (shrinks via the constant-product invariant), so callers no
longer need to thread "which side is being bought" through the AMM.

`placeBet` in `data/trading.ts` simplifies — the hardcoded `"A"` arg goes
away. `placeBetLegacy` (kept for the pre-multi-outcome integration test)
adapts by flipping the pool's YES/NO reserves before the call when outcome
is "B" and flipping the result back; same numerical behavior, just expressed
on a YES-only kernel. AMM unit tests updated to the new signature, including
the existing large-trade case (formerly outcome="B") which is now expressed
directly as a YES buy.

### Files changed
- `src/lib/amm.ts` (drop `outcome` param; rename target/other locals to
  `newReserveYes/No`)
- `src/data/trading.ts` (drop "A" arg in placeBet; flip-pool adapter in
  placeBetLegacy; import `AMMPool` for the flipped-newPool typing)
- `src/lib/amm.test.ts` (drop `outcome` arg from all `calculateBuy` calls;
  rewrite "large trade" case to use YES-buy directly)
- `PLAN.md` (mark Phase 1 line 59 complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing
  `vitest.integration.config.ts` overload error remains.
- `bun run test` — 40/40 unit tests pass.
- `bun run test:integration` — 3/3 tests pass (placeBetLegacy "A" path still
  works through the new flip adapter).
- `bunx oxlint` on changed files — 0 warnings, 0 errors.
- `bunx oxfmt --check` on changed files — clean (after `oxfmt` rewrap of
  the calculateBuy signature onto one line).

### Notes
- The flip-pool trick in `placeBetLegacy` is a temporary scaffold — the
  legacy adapter (and its sole consumer
  `tests/integration/bet-and-resolve.test.ts`) gets deleted in Phase 6 when
  the integration test is rewritten against the new YES-only `placeBet`.
- The `priceA/priceB` return shape of `calculatePrices`/`calculateOdds` is
  intentionally untouched here — that rename (Phase 1 lines 60–61) needs a
  coordinated UI sweep across `bet-controls`, `market-card`, `market-item`,
  `portfolio`, and `live-odds`.

## 2026-05-11 — Phase 1: Rename `calculatePrices`/`calculateOdds` to YES/NO

Renamed the display-only AMM return-shape keys from `priceA`/`priceB`/
`multiplierA`/`multiplierB` to `priceYes`/`priceNo`/`multiplierYes`/
`multiplierNo`. Under the YES-first mapping this is a pure key rename —
the underlying math was already correct (`priceA = reserveNo / total` is
the YES price; `priceB = reserveYes / total` is the NO price).

Scope was kept narrow per the "ONLY WORK ON A SINGLE TASK" instruction:
the AMM internal contract flips, but the consumer-facing `MarketWithOdds`
type (used across UI components) still carries `priceA/priceB`. The
boundary is `enrichMarket` in `data/markets.ts`, which now destructures
the new keys and remaps them back to A/B when building `MarketWithOdds`.
That keeps the UI rewiring (PLAN Phase 5) as a separate, coordinated
sweep instead of cascading 8 file edits into this task.

The internal `Odds` type in `data/trading.ts` (return-shape of `placeBet`)
was updated to the new YES/NO keys outright — confirmed via grep that
`PlaceBetResult.odds` has no external consumers, so flipping the field
names is safe.

### Files changed
- `src/lib/amm.ts` (calculatePrices and calculateOdds return YES/NO keys)
- `src/lib/amm.test.ts` (destructure priceYes/priceNo instead of priceA/B)
- `src/data/markets.ts` (enrichMarket explicitly maps calculateOdds keys
  to MarketWithOdds priceA/priceB/multiplierA/multiplierB)
- `src/data/trading.ts` (Odds type now { priceYes, priceNo, multiplierYes,
  multiplierNo })
- `PLAN.md` (mark Phase 1 lines 60-61 complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing `vitest.integration.config.ts`
  overload error remains.
- `bun run test` — 40/40 unit tests pass.
- `bun run test:integration` — 3/3 tests pass.
- `bunx oxlint` on changed files — 0 warnings, 0 errors.
- `bunx oxfmt --check` on changed files — clean.

### Notes
- The two PLAN bullets (`calculatePrices` rename + `calculateOdds` rename)
  were treated as one task because `calculateOdds` calls `calculatePrices`
  internally — splitting them would leave the intermediate state with a
  type mismatch.
- The boundary mapping in `enrichMarket` is intentional scaffolding. When
  Phase 5 UI rewiring lands, the mapping disappears: `MarketWithOdds`
  drops to `{ priceYes, multiplierYes, ... }` and components consume the
  YES-side directly (NO price is `1 - priceYes`).
  YES/NO keys directly.

## 2026-05-11 — Phase 1: Purge `"A" | "B"` from `Transaction` payload types

Removed legacy A/B fields from two variants of the `Transaction` discriminated
union in `src/lib/types.ts` per PLAN line 50:
- `PlaceBet`: dropped `outcome: "A" | "B"`. The new `placeBet` in
  `data/trading.ts` already omits `outcome` from its JSON payload — only the
  legacy adapter (`placeBetLegacy`) still writes it. Since `Transaction` is
  type-only and not imported anywhere, this change just aligns the documented
  shape with the YES-only model.
- `ResolveMarket`: replaced `result: "A" | "B"` with `resolvedAs: "yes" | "no"`
  to match the new `markets.resolvedAs` column. The dual-write in
  `data/events.ts:310` still emits both `result: legacyOutcome` and
  `resolvedAs: childOut.resolvedAs` in the actual payload — `JSON.stringify`
  isn't type-checked against `Transaction`, so the extra `result` field is
  harmless. Cleanup of the dual-write happens when the legacy A/B columns
  drop in Phase 1 schema migration.

`SellShares` was already absent from the union (it never made it past the
Slice 1 reshape), so that bullet of PLAN line 50 was already complete.
`SettlePayout` never had an `"A" | "B"` field.

### Files changed
- `src/lib/types.ts` (Transaction.PlaceBet drops `outcome`;
  Transaction.ResolveMarket swaps `result` for `resolvedAs`)
- `PLAN.md` (mark Phase 1 line 50 complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing
  `vitest.integration.config.ts` overload error remains.
- `bun run test` — 40/40 unit tests pass.
- `bun run test:integration` — 3/3 tests pass.
- `bunx oxlint src/lib/types.ts` — 0 warnings, 0 errors.
- `bunx oxfmt --check src/lib/types.ts` — clean.

### Notes
- `Transaction` has zero importers in src/ — confirmed via grep for
  `import.*Transaction.*types` and `: Transaction[^a-zA-Z]`. This makes the
  type a documentation artifact rather than a runtime contract, which is why
  the JSON.stringify call sites can carry extra fields (e.g. `result`)
  without TypeScript complaining. Future work could either delete the type
  entirely or wire it up as a typed bridge for the audit-log payload — both
  out of scope for this task.
- Kept the change strictly to PLAN line 50. The remaining `"A" | "B"`
  references elsewhere (`Market.result`, `SharePosition.outcome`,
  `MarketWithOdds.priceA/B`) are covered by separate PLAN bullets (lines
  51–53) and will land alongside their respective consumer rewrites.

## 2026-05-11 — Phase 1: Make `initialProbabilityYes` required on `initializePool`

PLAN line 58: tightened `initializePool`'s signature from
`(marketId, seedAmount, initialProbabilityYes?)` with a `?? 0.5` default to
`(marketId, seedAmount, initialProbabilityYes)` — required. The return shape
already matched the PLAN spec (`{ marketId, reserveYes, reserveNo, k,
liquidity }`).

Verified production callers (`data/events.ts:47`, `data/markets.ts:165`) and
all but one test were already passing the probability explicitly. The lone
default-relying test ("defaults to 0.5 when no probability is provided") was
deleted — under the new contract there is no implicit 0.5 to verify, and
every other test (`clamps probabilities to [0.01, 0.99]`, the pool-seeding
cases, and the calculateBuy/calculateOdds cases) already pins the
probability directly.

Rationale: the multi-outcome translator always derives
`initialProbabilityYes` from each child's own bid/ask midpoint, so there is
no production code path that would benefit from an implicit default. Making
the parameter required forces every caller (including future ones) to make
the seeding choice explicit.

### Files changed
- `src/lib/amm.ts` (drop `?` from parameter; inline the BigInt conversion;
  remove the `raw ?? 0.5` indirection)
- `src/lib/amm.test.ts` (delete the "defaults to 0.5" case)
- `PLAN.md` (mark Phase 1 line 58 complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing
  `vitest.integration.config.ts` overload error remains.
- `bun run test` — 39/39 unit tests pass (was 40 with the deleted default
  case).
- `bun run test:integration` — 3/3 tests pass.
- `bunx oxlint` on changed files — 0 warnings, 0 errors.
- `bunx oxfmt` on changed files — clean.

### Notes
- The default-removal could have been kept as a defensive guard, but the
  PLAN spec explicitly omits the `?` on the signature and we have no
  caller that wants to seed without an opinion on the YES probability.
  Better to fail at the type level than silently lie with a 50/50 seed
  when a translator forgets to thread its midpoint through.

## 2026-05-11 — Phase 3: Pure unit tests for `computeEventSettlement`

PLAN lines 132–135: added four `describe("computeEventSettlement –
per-child outcomes")` tests covering the pure-logic per-child outcomes
of the new event-level settlement function. Until now `settlement.test.ts`
only exercised the legacy `computeSettlement` (sharesA/sharesB) — the new
YES-only event-level path had zero direct unit coverage.

Coverage added:
- `resolved_yes` → YES holders get `amount = shares` win rows;
  `finalStatus = "resolved"`; `resolvedAs = "yes"`.
- `resolved_no` → every holder gets a zero-amount loss row (still emitted
  so the audit ledger shows the position cleared); `resolvedAs = "no"`.
- `cancelled_voided | cancelled_scalar | cancelled_no_settlement` →
  `costBasis` refund rows (or `zero` rows when costBasis was 0); covered
  via a loop over all three cancel outcomes since they share the same
  payout shape per ADR-0004.
- Per-child treasury backstop: a two-child input with one healthy and
  one under-collateralised child verifies the backstop fires only on the
  short child — `m-healthy.backstopAmount === 0n` while
  `m-short.backstopAmount === 300n`. Confirms the per-child accounting
  is isolated rather than netted across siblings.

PLAN lines 131 and 136 stay open because they describe the DB-transaction
effects of `commitEvent` (row counts, rollback semantics) — those belong
in the Phase 6 integration test bucket, not in `settlement.test.ts`.

### Files changed
- `src/lib/settlement.test.ts` (added `computeEventSettlement` describe
  block with four test cases + local `epos`/`child` helpers)
- `PLAN.md` (mark Phase 3 settlement tests lines 132–135 complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing
  `vitest.integration.config.ts` overload error remains.
- `bun run test` — 43/43 unit tests pass (was 39 before; +4 new cases).
- `bunx oxlint src/lib/settlement.test.ts` — 0 warnings, 0 errors.
- `bunx oxfmt src/lib/settlement.test.ts` — clean (formatter
  consolidated one wrapped line in the cancelled_* case).

### Notes
- Kept the cancelled_* test parameterised over all three outcomes via a
  loop. The settlement logic treats them identically (they all map to
  `isCancel`), so a single test verifies the contract without three
  copy-paste cases drifting apart over time.
- The under-collateralisation test specifically asserts independent
  per-child `backstopAmount` values rather than just a positive total —
  this catches a regression where siblings might net their wpmReserve
  against each other's payouts, which would violate the per-child
  accounting in `settleChild`.
- Did not test `commitEvent` directly because that requires DB plumbing
  (transactions, balance row writes, status flips) and belongs in
  `tests/integration/`. The pure settlement function is the right
  surface for these property assertions.

## 2026-05-11 — Phase 3: AMM tests cleanup

PLAN lines 139–143: filled in the four missing AMM unit tests called for by
the multi-outcome plan and confirmed line 139 (reserveYes/No naming, no
`outcome` param on `calculateBuy`) was already true of every existing case.

Coverage added:
- `initializePool` "implied probability tracks the seed within one unit":
  parameterised over five YES probabilities, confirms total reserves equal
  `2 * seed` (no rounding leak) and that `reserveNo / total` reproduces the
  requested probability within `1/total` — i.e. the integer truncation cost
  is bounded by exactly one unit.
- `calculateBuy` "does not under-pay or panic when YES reserve is at the
  floor": seeds at the clamped extreme (0.99 → reserveYes ≈ 20) and sweeps
  with a 100k buy. Asserts shares > deposit (the AMM still honours a swap
  out), reserveYes stays positive but strictly shrinks, and `k` is preserved
  via the constant-product invariant.
- `calculatePrices` describe block (new): one parameterised case across five
  skews verifying `priceYes + priceNo === 1` to ten decimal places, plus a
  post-trade case asserting the sum still holds and `priceYes` moved past
  0.5 after a YES buy.

The four "calculateOdds" tests already covered the prices-sum-to-1 invariant
indirectly (since `calculateOdds` wraps `calculatePrices`), but the PLAN
asks for an explicit test on `calculatePrices` itself — added rather than
just leaning on the wrapper.

### Files changed
- `src/lib/amm.test.ts` (added "implied probability" case in initializePool;
  added degenerate-pool case in calculateBuy; added new `calculatePrices`
  describe block with two cases)
- `PLAN.md` (mark Phase 3 AMM tests lines 139–143 complete)

### Verification
- `bun run test` — 47/47 pass (was 43; +4 new cases).
- `bunx tsc --noEmit` — only the pre-existing
  `vitest.integration.config.ts` overload error remains.
- `bunx oxlint src/lib/amm.test.ts` — 0 warnings, 0 errors.
- `bunx oxfmt --check src/lib/amm.test.ts` — clean.

### Notes
- The "within one unit" wording in PLAN line 140 is about the implied
  probability, not the reserve magnitudes — `floor((2*seed * (1-p)))` can be
  off by at most 1 from the real-valued target, which translates to a
  probability error of at most `1/total`. Pinned exactly that bound rather
  than a looser tolerance so the test would fail loudly if anyone widened
  the rounding (e.g. by switching to ceil-div on init).
- Picked `0.99` for the degenerate test because the clamp pushes any
  `p >= 0.99` into the same MIN_PROB_BPS edge — no need to test 1.0
  separately, since clampProbBps collapses both inputs to the same pool.
- `calculateBuy` against an extreme pool: 100k WPM into reserveNo ≈ 1980
  drives newReserveYes via `ceilDiv(k, newReserveNo)` to a tiny number but
  never zero, since k > 0 and ceil-div rounds the retained YES side UP. The
  test pins both invariants so a future refactor can't silently switch to
  floor-div or non-bigint math.

## 2026-05-11 — Phase 2: Translator test for arbitrage-prone midpoints

PLAN line 85: added explicit test verifying that two children with
arbitrage-prone YES midpoints (both at 0.70 — implied probabilities summing
to 1.40) translate to `ok` rather than being rejected. Under the old binary
model this was the canonical `pair_inconsistent` case; under the new
multi-outcome model each child is an independent YES/NO contract and the
disagreement is not a fatal inconsistency.

The existing `accepts a skewed but healthy event` test (using
`skewed-healthy.json` at 0.86 / 0.14, summing to 1.00) already exercised
the skewed-but-consistent path, but didn't cover the disagreement case
that would have tripped `pair_inconsistent`. The new test pins that
behavior down with an inline-mutated fixture so future translator changes
can't silently re-introduce a binary-style consistency check.

### Files changed
- `src/lib/kalshi/translator.test.ts` (added one `it` block constructing
  an inline arbitrage event derived from `binary-healthy.json`)
- `PLAN.md` (mark Phase 2 translator tests line 85 complete)

### Verification
- `bunx vitest run src/lib/kalshi/translator.test.ts` — 11/11 pass (was 10).
- `bun run test` — 48/48 unit tests pass (was 47).
- `bunx tsc --noEmit` — only the pre-existing
  `vitest.integration.config.ts` overload error remains.
- `bunx oxlint src/lib/kalshi/translator.test.ts` — 0 warnings, 0 errors.
- `bunx oxfmt --check src/lib/kalshi/translator.test.ts` — clean.

### Notes
- Chose 0.70 / 0.70 (mid 1.40) rather than the symmetric 0.86 / 0.14 of
  `skewed-healthy.json` because the test's purpose is to pin the
  *disagreement* path, not the skew path. Two children both claiming 70%
  is the textbook arbitrage scenario the old `pair_inconsistent` check
  was guarding against.
- Built the fixture inline (mutating `binary-healthy.json`) rather than
  adding a new JSON file. The test exercises one specific number tuple
  and a standalone fixture would be over-engineered.

## 2026-05-11 — Phase 2: Translator row shapes pinned to schema insert types

PLAN line 86: added compile-time + runtime checks that the translator's
`TranslatedEventRow` and `TranslatedMarketRow` stay aligned with the drizzle
`$inferInsert` shapes for `events` and `markets`. Two layers:

- Top-of-file `AssertEqual<A,B>` type identity checks pin the translator's
  exported row types to `Omit<events.$inferInsert, "status" | "createdAt">`
  and `Omit<markets.$inferInsert, "status" | "createdAt" | "eventId">`. The
  identity helper is the standard distributive `<T>() => T extends X ? 1 : 2`
  trick, which detects narrowing or widening in either direction (e.g. a
  schema column renamed but translator unchanged, or vice versa).
- A new runtime test feeds a real `translateKalshiEvent` output through the
  ingest-shaped composition (translator row + `status`/`createdAt` + `eventId`
  for child markets) and assigns it to `typeof eventsTable.$inferInsert` /
  `typeof marketsTable.$inferInsert`. If anyone reshapes the translator
  without updating the schema (or vice versa), tsc fails this test file before
  vitest collects.

The type-only `AssertEqual` constants are bound to runtime variables and
discharged via `void` so oxlint doesn't flag them as unused. This is the
conventional pattern for compile-time-only assertions in vitest files.

### Files changed
- `src/lib/kalshi/translator.test.ts` (added `AssertEqual` type identities for
  both row types and a runtime "produces row shapes that satisfy the
  schema-derived insert types" test case)
- `PLAN.md` (mark Phase 2 translator tests line 86 complete)

### Verification
- `bunx tsc --noEmit` — only the pre-existing
  `vitest.integration.config.ts` overload error remains.
- `bunx vitest run src/lib/kalshi/translator.test.ts` — 12/12 pass (was 11).
- `bun run test` — 49/49 unit tests pass (was 48).
- `bunx oxlint src/lib/kalshi/translator.test.ts` — 0 warnings, 0 errors.
- `bunx oxfmt src/lib/kalshi/translator.test.ts` — clean (formatter
  reordered the new `events`/`markets` schema import into the right block).

### Notes
- Chose two complementary assertion styles deliberately. The type-only
  `AssertEqual` catches structural drift even if no test ever runs the
  translator (e.g. a column rename in `app.ts` that breaks the `Omit` math).
  The runtime assignment additionally verifies that a real translator output
  literal (post-composition with the ingest-added fields) is assignable to
  the insert type — which catches drift in the opposite direction (translator
  producing a value that's wider than its declared row type).
- The `Omit` keys (`"status" | "createdAt"` and `"status" | "createdAt" |
  "eventId"`) intentionally match the literal omits in `translator.ts`. If
  someone later moves `closesAt` or `sport` out of the translator's output
  (say, into the ingest driver), the assertion forces them to update both
  sides in lockstep — exactly the contract the PLAN wanted pinned down.
- Did not use vitest's `expectTypeOf` because it doesn't ship with the
  distributive equality check needed here (`toEqualTypeOf` accepts subtypes).
  The `<T>() => T extends X ? 1 : 2` trick is the standard rigorous
  alternative.

## 2026-05-11 — Phase 3: PLAN audit, mark resolver decision core complete

PLAN lines 99, 100, 101, 104: the resolver decision core in
`src/lib/kalshi/resolve.ts` was already implemented (the corresponding test
checkboxes at lines 114–119 were marked complete back when the
`decideEventCommit` tests landed), but the implementation lines for the
function itself had been left unchecked. Verified line-by-line against the
current code and flipped them to `[x]`.

Verification points:
- Line 99 (`decideEventCommit` pure function with `wait | commit` return) —
  `resolve.ts:44-105` matches the exact signature and discriminated union.
- Line 100 (`ChildOutcome` shape with five outcome kinds) — `resolve.ts:19-27`
  declares the full union.
- Line 101 (happy-path mapping of Kalshi `result` to
  `resolved_yes`/`resolved_no`/`cancelled_scalar`) — `resolve.ts:74-92`
  implements the switch with a `wait`-fallback for unrecognised pre-deadline
  results.
- Line 104 (wait when any child non-terminal and deadline not reached) —
  `resolve.ts:62-72` returns `{ kind: 'wait' }` exactly in that case.

### Files changed
- `PLAN.md` (mark Phase 3 resolver decision core lines 99/100/101/104
  complete)

### Verification
- `bun run test` — 49/49 unit tests pass (unchanged).
- `bunx tsc --noEmit` — only the pre-existing
  `vitest.integration.config.ts` overload error remains.
- `bunx oxfmt --check PLAN.md` — fails, but the failure pre-dates this
  edit (PLAN.md has long-standing formatter drift; not in scope here).

### Notes
- Scoped this audit to a single coherent cluster (one function's contract +
  its decision-tree branches) rather than sweeping all of Phase 2/3 in one
  pass. Several other PLAN items also appear to be done-but-unchecked
  (Phase 2 translator return type, Phase 2 ingest SkipReasonKind union,
  Phase 3 resolver driver, Phase 3 settlement core) — leaving those for
  future iterations to verify and flip individually so each commit stays
  small and auditable.
- The void-semantics line 102 was already `[x]`. Re-verified against
  `resolve.ts:97-102` (rewrites every outcome to `cancelled_voided` when
  every child settled `no`); kept as-is.

## 2026-05-11 — Phase 2: PLAN audit, mark translator implementation cluster complete

PLAN lines 69, 70, 73, 74, 75: the translator implementation at
`src/lib/kalshi/translator.ts` already matched the PRD §Translator spec
(the corresponding test checkboxes are partly checked too — see lines 85,
86, 87 — but the implementation lines themselves had been left unchecked).
Verified each against the current code and flipped them to `[x]`.

Verification points:
- Line 69 (new return type with `{ kind: 'ok', value: { event, markets:
  TranslatedMarket[] } }`) — `translator.ts:26-43` declares
  `TranslatedMarket`, `TranslatedEvent`, and the `ok` variant exactly.
- Line 70 (drop `non_binary` and `pair_inconsistent` variants) —
  `TranslationResult` at `translator.ts:42-57` contains only the six
  variants the new model uses; the two legacy variants are absent.
- Line 73 (per-Market spread gate aggregating reasons; event-level
  `no_initial_price`) — `translator.ts:89-106` collects per-market
  `{ ticker, reason: 'spread_too_wide' }` tuples and returns
  `insufficient_confidence` with the aggregated array; the no-quote case
  returns `no_initial_price` at the event level (the variant shape has no
  per-child reasons field — collective signal is the contract).
- Line 74 (closesAt sourced from `markets[0].expected_expiration_time`,
  siblings must match) — `translator.ts:70` reads the first child's
  timestamp; `translator.ts:76-87` verifies siblings and returns
  `inconsistent_close_times` with offender tickers if any disagree.
- Line 75 (derive `TranslatedEventRow`/`TranslatedMarketRow` from drizzle
  `$inferInsert`) — `translator.ts:20-24` defines both via `$inferInsert`
  on the schema tables, with the `Omit` keys (`status`/`createdAt`,
  plus `eventId` for markets) matching the ingest-fills-it-in contract.

### Files changed
- `PLAN.md` (mark Phase 2 translator implementation lines 69, 70, 73, 74,
  75 complete)

### Verification
- `bun run test` — 49/49 unit tests pass (unchanged).
- `bunx tsc --noEmit` — only the pre-existing
  `vitest.integration.config.ts` overload error remains.
- `bunx oxlint src/lib/kalshi/translator.ts` — 0 warnings, 0 errors.

### Notes
- Followed the prev-turn audit pattern: pick one coherent cluster (the
  translator's complete return-type / variant-set / spec-derived row types)
  rather than sweeping every Phase 2 line. Several other PLAN items are
  also done-but-unchecked (Phase 2 translator tests lines 79, 81-84;
  Phase 2 ingest SkipReasonKind union at line 90; Phase 3 settlement
  core) — leaving those for future iterations so each commit stays small
  and auditable.
- Held some ambiguity on line 73's "Aggregate `no_initial_price` if any
  child has no usable quote" — read it as "the gate's collective outcome
  is `no_initial_price`" (event-level signal), not "aggregate per-child
  reasons inside the variant". The variant's shape (`{ kind, eventTicker
  }` with no reasons field) and the existing test
  (`translator.test.ts:128-135`) both back this reading.
