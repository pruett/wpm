## 2026-03-07 — Phase 0, TB1, Task 1: Add Hono dependency

- Added `hono@^4.12.5` to `packages/api/package.json` dependencies
- Updated scripts to use Bun runtime (`bun --watch`, `bun src/index.ts`, `bun test`) instead of tsx/node/vitest
- SQLite handled by `bun:sqlite` built-in — no external dep needed

Files changed:
- packages/api/package.json
- bun.lock

## 2026-03-07 — Phase 0, TB1, Task 2: Create index.ts with Hono app + /health

- Created `packages/api/src/index.ts` — Hono app entry point
- `GET /health` proxies node `/internal/health` with `AbortSignal.timeout(5000)`, returns API uptime + nodeReachable boolean + node stats when reachable
- Returns `"degraded"` status when node is unreachable, `"ok"` otherwise
- Served via `Bun.serve()` on `API_PORT` env (default 3000), `NODE_URL` env (default `http://localhost:3001`)
- Added `@types/bun` dev dependency and `"types": ["bun"]` to api tsconfig for Bun global types
- Exports `app` and `server` for testing

Files changed:
- packages/api/src/index.ts (new)
- packages/api/tsconfig.json (added bun types)
- packages/api/package.json (added @types/bun)
- bun.lock

## 2026-03-07 — Phase 0, TB1, Task 3: Create node-client.ts

- Created `packages/api/src/node-client.ts` — typed HTTP client wrapping all node internal endpoints
- Uses Bun-native `fetch` with `AbortSignal.timeout(5000)` for all requests
- Returns discriminated union `NodeResult<T>` — `{ ok: true, data: T }` or `{ ok: false, error, status }`
- Returns `NODE_UNAVAILABLE` (503) on network failures / timeouts
- Wraps all 9 endpoints: `getHealth`, `getBalance`, `getState`, `getMarket`, `getShares`, `getBlocks`, `getBlock`, `submitTransaction`, `distribute`, `referralReward`
- Exports `createNodeClient(baseUrl)` factory and `NodeClient` type
- URL-encodes path parameters (address, marketId) for safety

Files changed:
- packages/api/src/node-client.ts (new)
- PLAN.md (marked task complete)

## 2026-03-07 — Phase 0, TB1, Task 4: Create db/index.ts — SQLite init + users table

- Created `packages/api/src/db/index.ts` — SQLite database module using `bun:sqlite`
- `openDatabase(path)` factory: enables WAL mode, foreign keys, 5s busy timeout, then runs migrations
- `getDb()` singleton accessor with lazy init, uses `DB_PATH` env (default `wpm-api.db`)
- `closeDb()` for graceful shutdown
- `users` table: id (TEXT PK), name, email (UNIQUE COLLATE NOCASE), wallet_address (UNIQUE), wallet_private_key_enc (BLOB), role (DEFAULT 'user'), created_at (INTEGER)
- Email uniqueness is case-insensitive via `COLLATE NOCASE`
- Created `packages/api/tests/db.test.ts` — 7 tests covering WAL mode, foreign keys, column schema, case-insensitive email uniqueness, wallet_address uniqueness, insert/query round-trip, idempotent migration

Files changed:
- packages/api/src/db/index.ts (new)
- packages/api/tests/db.test.ts (new)
- PLAN.md (marked task complete)

## 2026-03-07 — Phase 0, TB1, Task 5: Create crypto/wallet.ts — wallet key generation + encryption

- Created `packages/api/src/crypto/wallet.ts` — wallet key management module
- `generateWalletKeyPair()` delegates to `@wpm/shared/crypto` `generateKeyPair()` (RSA-2048)
- `encryptPrivateKey(key, secret)` — AES-256-GCM encryption using Web Crypto API (`crypto.subtle`)
- `decryptPrivateKey(enc, secret)` — AES-256-GCM decryption, returns original PEM string
- Key derivation: SHA-256 hash of secret via `Bun.CryptoHasher` → 32-byte AES key
- Random 12-byte IV per encryption; format: `[IV (12 bytes)][ciphertext + GCM auth tag]`
- Decision: did NOT modify `@wpm/shared/crypto` — `@wpm/node` deeply depends on it (all tx/block validation). Used Bun-native APIs in the API layer only.
- Created `packages/api/tests/crypto-wallet.test.ts` — 8 tests covering key generation, round-trip encrypt/decrypt, random IV uniqueness, wrong secret rejection, tampered/truncated ciphertext rejection

Gotchas:
- Bun's `Buffer.buffer` returns `ArrayBufferLike` (union of `ArrayBuffer | SharedArrayBuffer`), needs cast to `ArrayBuffer` for Web Crypto API compatibility
- `Bun.CryptoHasher.digest()` returns `Buffer`, not `Uint8Array` — extract underlying `ArrayBuffer` with `.buffer` + `.slice()`

Files changed:
- packages/api/src/crypto/wallet.ts (new)
- packages/api/tests/crypto-wallet.test.ts (new)
- PLAN.md (marked task complete)

## 2026-03-07 — Phase 0, TB1, Task 6: Create middleware/auth.ts — JWT auth

- Created `packages/api/src/middleware/auth.ts` — JWT signing, verification, and Hono auth middleware
- `signJwt(payload)` — signs JWT with HMAC-SHA256 via Hono's `hono/jwt` utilities, keyed by `JWT_SECRET` env var
- `verifyJwt(token)` — verifies signature + expiration, returns typed `JwtUserPayload`
- `authMiddleware` — Hono middleware extracting `Bearer` token from `Authorization` header, verifies JWT, sets `c.set("user", payload)` on context
- `JwtUserPayload` type: `sub`, `role` ("user" | "admin"), optional `walletAddress` + `email`, `iat`, `exp`
- Default access token TTL: 15 minutes; custom `exp` supported for admin tokens (24h)
- Returns structured `{ error: { code: "UNAUTHORIZED", message } }` JSON on all 401 paths
- No new dependencies — leverages Hono's built-in `hono/jwt` (`sign`/`verify`)
- Created `packages/api/tests/auth.test.ts` — 11 tests covering round-trip sign/verify, custom expiry, expired token rejection, tampered token rejection, wrong secret rejection, missing JWT_SECRET, middleware integration (valid token, missing header, malformed header, invalid token, expired token)

Files changed:
- packages/api/src/middleware/auth.ts (new)
- packages/api/tests/auth.test.ts (new)
- PLAN.md (marked task complete)

## 2026-03-08 — Phase 0, TB1, Task 7: Create errors.ts — typed error helper + catalog

- Created `packages/api/src/errors.ts` — typed error helper and error catalog matching spec Section 7
- `ERRORS` constant maps all 19 error codes to their default HTTP status and message
- `apiError(code, message?)` builds the `{ error: { code, message } }` envelope
- `sendError(c, code, message?, status?)` sends a JSON error response from Hono context with correct HTTP status
- Exports `ErrorCode` type for type-safe error code usage across the codebase
- Uses Hono's `ContentfulStatusCode` for proper type compatibility with `c.json()`
- Created `packages/api/tests/errors.test.ts` — 8 tests covering: default message, custom message, all codes structure, catalog completeness (19 codes), HTTP status mapping, sendError integration with correct status/body/custom message/status override

Gotchas:
- Hono's `c.json()` requires `ContentfulStatusCode` not `StatusCode` — the latter includes informational (1xx) codes that aren't valid for JSON responses

Files changed:
- packages/api/src/errors.ts (new)
- packages/api/tests/errors.test.ts (new)
- PLAN.md (marked task complete)
