## Phase 0 — SSR Read Tracer

### Task 1: Scaffold apps/web workspace
- Added `"apps/*"` to root `package.json` workspaces array
- Created `apps/web/package.json` with Next 16.2.3, React 19.2.5, `@wpm/shared: workspace:*`
- Used temporary name `@wpm/web-next` to avoid conflict with existing `packages/web` (`@wpm/web`) — will rename during Phase 6 cutover
- Verified workspace resolves correctly via `bun install`
- Files changed: `package.json`, `apps/web/package.json`, `bun.lock`

### Task 2: Create tsconfig.json and next.config.ts
- Created `apps/web/tsconfig.json` with Next.js conventions: `@/*` path alias → `./src/*`, `next` plugin, `skipLibCheck`, `jsx: "preserve"`, `incremental`
- Created `apps/web/next.config.ts` with `cacheComponents: true` (enables `'use cache'` directive) and `output: "standalone"` (required for Docker)
- Type check passes cleanly with `tsc --project tsconfig.json --noEmit`
- `next build` correctly reports missing `app` directory (expected — created in next task)
- Files changed: `apps/web/tsconfig.json`, `apps/web/next.config.ts`

### Task 3: Create layout.tsx and page.tsx
- Created `apps/web/src/app/layout.tsx` — minimal root layout with `<html>`, `<body>`, metadata (title: "WPM", description: "Wampum Prediction Markets")
- Created `apps/web/src/app/page.tsx` — renders hardcoded "WPM" heading and "Wampum Prediction Markets" tagline
- TypeScript type check passes cleanly
- `next build` succeeds — generates static `/` route
- Note: Next.js auto-updated `tsconfig.json` to set `jsx: "react-jsx"` (required by Next's automatic runtime) and added `.next/dev/types/**/*.ts` to includes
- Files changed: `apps/web/src/app/layout.tsx`, `apps/web/src/app/page.tsx`, `apps/web/tsconfig.json` (auto-modified by Next)

### Task 4: Add cached getMarkets() loader
- Created `apps/web/src/lib/data/markets.ts` with `'use cache'` directive
- Uses `cacheLife("minutes")` and `cacheTag("markets")` from `next/cache` for cache lifetime and tag-based invalidation
- Fetches from `WPM_API_URL` env var (defaults to `http://localhost:4101` matching `API_PORT` from `@wpm/shared` constants)
- Returns typed `MarketsResponse` from `@wpm/shared` (includes `active: string[]` and `markets: MarketWithOdds[]`)
- TypeScript type check and `next build` both pass cleanly
- Files changed: `apps/web/src/lib/data/markets.ts` (new), `PLAN.md`

### Task 5: Render market names in Dashboard server component
- Created `apps/web/src/app/dashboard.tsx` — async server component calling `getMarkets()` and rendering market names with outcome probabilities
- Uses `connection()` from `next/server` to opt into dynamic rendering (prevents build-time prerendering when API is unavailable)
- Updated `apps/web/src/app/page.tsx` — branches on hardcoded `authenticated=true` flag: shows `<Dashboard />` wrapped in `<Suspense>` when true, landing text when false
- Next.js 16 Partial Prerender kicks in: page shell is static, Dashboard streams dynamically at request time
- TypeScript type check and `next build` both pass cleanly
- Files changed: `apps/web/src/app/dashboard.tsx` (new), `apps/web/src/app/page.tsx`, `PLAN.md`
- Gotcha: Next.js 16 with `cacheComponents: true` requires uncached data access (like `connection()`) to be inside a `<Suspense>` boundary, otherwise the build errors with "Uncached data was accessed outside of <Suspense>"

### Task 6: Author apps/web/Dockerfile
- Created `apps/web/Dockerfile` with two stages: `builder` (node:22-alpine) and runtime (node:22-alpine)
- Builder stage: copies workspace package.jsons, converts bun `workspace:*` protocol via sed, runs `npm install`, then `turbo build --filter=@wpm/web-next`
- Runtime stage: copies Next.js standalone output (`.next/standalone`) and static assets (`.next/static`), runs as non-root `nextjs` user
- Port 4102, `HOSTNAME=0.0.0.0` for container networking
- Added `**/.next` to `.dockerignore` to prevent local build cache from bloating Docker context
- Follows existing monorepo Dockerfile patterns (sed workspace protocol, npm install, turbo for build orchestration)
- Files changed: `apps/web/Dockerfile` (new), `.dockerignore`

### Task 7: Swap docker-compose.yml to use apps/web/Dockerfile
- Changed `wpm-web` service `dockerfile` from `packages/web/Dockerfile` to `apps/web/Dockerfile`
- Renamed env var `API_URL` to `WPM_API_URL` to match what `apps/web/src/lib/data/markets.ts` reads (`process.env.WPM_API_URL`)
- Kept all auth-related env vars (`BETTER_AUTH_SECRET`, `BETTER_AUTH_URL`, `RESEND_API_KEY`, `RP_ID`, `ORIGIN`) for Phase 1 compatibility
- Kept `auth-data:/app/data` volume mount for future better-auth integration
- Validated with `docker compose config --quiet` — syntax is clean (env var warnings expected without .env)
- Files changed: `docker-compose.yml`, `PLAN.md`

## Phase 0 — SSE Proxy Tracer

### Task 8: SSE proxy route, RealtimeProvider, and LiveTicker
- Created `apps/web/src/app/api/stream/route.ts` — proxies upstream `WPM_API_URL/events/stream` SSE connection, piping the `ReadableStream` body directly with `text/event-stream` headers
- Uses `connection()` from `next/server` to opt into dynamic rendering (route segment config `dynamic` is incompatible with `cacheComponents: true`)
- Created `apps/web/src/lib/realtime/RealtimeProvider.tsx` — client component opening a single `EventSource('/api/stream')`, listening for `price:update`, `market:resolved`, and `balance:update` events, publishing parsed payloads into React context
- Created `apps/web/src/app/live-ticker.tsx` — throwaway client component displaying connection status and last received SSE event payload as JSON
- Updated `apps/web/src/app/page.tsx` to wrap authenticated content in `<RealtimeProvider>` and mount `<LiveTicker />` alongside the dashboard
- TypeScript type check and `next build` both pass cleanly
- Files changed: `apps/web/src/app/api/stream/route.ts` (new), `apps/web/src/lib/realtime/RealtimeProvider.tsx` (new), `apps/web/src/app/live-ticker.tsx` (new), `apps/web/src/app/page.tsx`, `PLAN.md`
- Gotcha: Next.js 16 with `cacheComponents: true` disallows route segment config exports (`export const dynamic = ...`). Use `connection()` instead to signal dynamic rendering in route handlers.

## Phase 1 — Authentication

### Task 9: Create better-auth server instance
- Created `apps/web/src/lib/auth.ts` — better-auth instance with `better-sqlite3` adapter
- Database path configurable via `DATABASE_PATH` env var, defaults to `data/wpm.db` (works for both local dev and containerized: Docker volume mounts at `/app/data`)
- `BETTER_AUTH_URL` defaults to `http://localhost:4102` (Next.js dev server port)
- `BETTER_AUTH_SECRET` for session signing
- Added `better-auth` (1.6.3), `better-sqlite3` (12.9.0), and `@types/better-sqlite3` as dependencies
- TypeScript type check and `next build` both pass cleanly
- Files changed: `apps/web/src/lib/auth.ts` (new), `apps/web/package.json`, `bun.lock`, `PLAN.md`

### Task 10: Register passkey and magic-link plugins with Resend email driver
- Updated `apps/web/src/lib/auth.ts` to register `magicLink` and `passkey` plugins
- `magicLink` plugin: logs magic link URL to console in development, sends email via Resend in production (same pattern as `packages/web`)
- `passkey` plugin: `RP_ID` (default `localhost`), `rpName` `"WPM"`, `ORIGIN` (default `http://localhost:4102`)
- Created `apps/web/src/lib/email.ts` — lazy-initialized Resend client with `RESEND_API_KEY` env var, `EMAIL_FROM` defaults to `"WPM <noreply@example.com>"`
- Added `@better-auth/passkey` (1.6.3) and `resend` (6.11.0) as dependencies
- Dropped invite code validation from magic-link handler (not in scope for apps/web plan)
- TypeScript type check and `next build` both pass cleanly
- Files changed: `apps/web/src/lib/auth.ts`, `apps/web/src/lib/email.ts` (new), `apps/web/package.json`, `bun.lock`, `PLAN.md`

### Task 11: Extend user schema with walletPublicKey field
- Added `user.additionalFields.walletPublicKey` to the betterAuth config in `apps/web/src/lib/auth.ts`
- Field config: `type: "string"`, `required: false` (nullable), `input: false` (server-only, clients cannot set during signup), `returned: true` (included in session responses)
- Immutability enforced at application level (in future `provisionWalletIfNeeded` function), not schema level — better-auth has no built-in "immutable once set" constraint
- `input: false` prevents client-side mutation, ensuring only server code can write this field
- TypeScript type check and `next build` both pass cleanly
- Files changed: `apps/web/src/lib/auth.ts`, `PLAN.md`

### Task 12: Create better-auth catch-all handler + port magic-link email template
- Created `apps/web/src/app/api/auth/[...all]/route.ts` — exports `GET` and `POST` via `toNextJsHandler(auth)` from `better-auth/next-js`
- This is the standard better-auth Next.js integration: all auth requests under `/api/auth/*` are routed to the better-auth instance
- Magic-link email template was already ported as-is during Task 10 (inline HTML `<a href="${url}">Click here to sign in to WPM</a>` with Resend driver) — marking complete
- TypeScript type check passes cleanly; `next build` succeeds with `/api/auth/[...all]` registered as dynamic route (`ƒ`)
- Files changed: `apps/web/src/app/api/auth/[...all]/route.ts` (new), `PLAN.md`
- Gotcha: `better-sqlite3` native binding errors during local `next build` are expected — the native addon resolves correctly in Docker where it's compiled against the container's Node ABI

## Phase 1 — Browser Auth Client

### Task 13: Create browser auth client
- Created `apps/web/src/lib/auth-client.ts` — better-auth React client with `magicLinkClient` and `passkeyClient` plugins
- Uses `createAuthClient` from `better-auth/react` (React-specific variant provides `useSession` hook and other React hooks)
- Registers `magicLinkClient()` from `better-auth/client/plugins` — exposes `authClient.signIn.magicLink()` for email-based auth
- Registers `passkeyClient()` from `@better-auth/passkey/client` — exposes `authClient.signIn.passkey()` and `authClient.passkey.addPasskey()` for WebAuthn
- Mirrors the pattern from `packages/web/src/lib/auth-client.ts` (Svelte version uses `createAuthClient` from `better-auth/svelte`)
- No `baseURL` needed — defaults to same origin, which is correct since the catch-all route handler is at `/api/auth/[...all]`
- TypeScript type check and `next build` both pass cleanly
- Files changed: `apps/web/src/lib/auth-client.ts` (new), `PLAN.md`

### Task 14: Build (auth)/layout.tsx route group with shadcn foundation
- Set up Tailwind CSS v4: installed `tailwindcss@4.2.2`, `@tailwindcss/postcss@4.2.2`; created `postcss.config.mjs` with `@tailwindcss/postcss` plugin
- Created `src/app/globals.css` with `@import "tailwindcss"` and `@theme` block matching the Svelte app's dark color palette (oklch-based: background, foreground, card, primary, secondary, muted, accent, destructive, border, input, ring, radius tokens)
- Set up shadcn/ui foundation: created `components.json` (new-york style, RSC enabled, lucide icons), `src/lib/utils.ts` with `cn()` helper (clsx + tailwind-merge)
- Installed shadcn dependencies: `class-variance-authority`, `clsx`, `tailwind-merge`, `lucide-react`, `@radix-ui/react-slot`, `@radix-ui/react-separator`
- Added shadcn UI components: `button.tsx` (6 variants, 4 sizes, asChild support), `card.tsx` (Card, CardHeader, CardTitle, CardDescription, CardContent, CardFooter), `input.tsx`, `separator.tsx` — all in `src/components/ui/`
- Created `src/app/(auth)/layout.tsx` — route group layout centering children vertically (`min-h-svh`) with `max-w-sm` container, serves as visual foundation for login/register/verify pages
- Updated `src/app/layout.tsx` — imports `globals.css`, adds Geist + Geist Mono fonts via `next/font/google` with CSS variable strategy, sets `dark` class on `<html>`, applies `font-sans antialiased` to `<body>`
- TypeScript type check and `next build` both pass cleanly
- Files changed: `apps/web/postcss.config.mjs` (new), `apps/web/src/app/globals.css` (new), `apps/web/components.json` (new), `apps/web/src/lib/utils.ts` (new), `apps/web/src/components/ui/button.tsx` (new), `apps/web/src/components/ui/card.tsx` (new), `apps/web/src/components/ui/input.tsx` (new), `apps/web/src/components/ui/separator.tsx` (new), `apps/web/src/app/(auth)/layout.tsx` (new), `apps/web/src/app/layout.tsx`, `apps/web/package.json`, `bun.lock`, `PLAN.md`
- Gotcha: Tailwind v4 uses CSS-based config (`@theme` in globals.css) — no `tailwind.config.ts` needed. next/font's `variable` prop sets CSS custom properties that Tailwind v4's default `font-sans` utility automatically picks up via `var(--font-sans)`.

### Task 15: Implement /login/page.tsx
- Created `apps/web/src/app/(auth)/login/page.tsx` — client component implementing the login flow
- Two auth methods: passkey-primary CTA (top, default variant) and magic-link secondary CTA (outline variant below separator)
- Passkey flow: calls `authClient.signIn.passkey()`, navigates to `/` on success via `useRouter`
- Magic-link flow: email input + submit, calls `authClient.signIn.magicLink({ email, callbackURL: "/auth/verify" })`, shows confirmation message with user's email on success
- Error handling: catches auth errors and displays them in destructive text
- Loading state: disables all inputs/buttons during submission, shows "Sending..." on magic link button
- Footer links to `/register` for new account creation
- Mirrors the Svelte login page (`packages/web/src/routes/(auth)/login/+page.svelte`) pattern exactly, adapted for React/Next.js (useState, useRouter, React.FormEvent)
- TypeScript type check passes cleanly
- Files changed: `apps/web/src/app/(auth)/login/page.tsx` (new), `PLAN.md`

### Task 16: Implement /register/page.tsx
- Created `apps/web/src/app/(auth)/register/page.tsx` — client component implementing the registration flow
- Name + email form calling `authClient.signIn.magicLink({ email, name, callbackURL: "/auth/verify" })`
- On success: shows confirmation message with user's email (same pattern as login page magic link flow)
- Error handling: catches auth errors and displays them in destructive text
- Loading state: disables all inputs/buttons during submission, shows "Sending..." on submit button
- Button disabled when name or email is empty
- Footer links to `/login` for existing accounts
- Mirrors the Svelte signup page (`packages/web/src/routes/(auth)/signup/+page.svelte`) pattern, adapted for React/Next.js
- Dropped invite code validation (not in scope per plan)
- TypeScript type check passes cleanly; build font-fetch errors are sandbox network restrictions only
- Files changed: `apps/web/src/app/(auth)/register/page.tsx` (new), `PLAN.md`

### Task 17: Implement /auth/verify/page.tsx
- Created `apps/web/src/app/(auth)/auth/verify/page.tsx` — client component completing the magic-link verification flow
- Three-stage state machine: `verifying` → `passkey-prompt` → redirect to `/` (or `error` if no session found)
- **Verifying stage**: reads session via `authClient.useSession()` hook from `better-auth/react`; waits up to 3 seconds for session to appear after magic-link redirect
- **Passkey prompt stage**: once session is detected, offers passkey enrollment via `authClient.passkey.addPasskey({ name: "WPM Passkey" })` with "Add passkey" primary CTA and "Skip for now" ghost button
- **Error stage**: if no session after timeout, shows "Verification failed" with "Back to login" link
- Passkey enrollment errors are silently swallowed (enrollment is optional) — redirects to `/` in the `finally` block regardless of success/failure
- Mirrors the Svelte verify page (`packages/web/src/routes/auth/verify/+page.svelte`) flow exactly, adapted for React/Next.js (useState, useEffect, useRouter, useSession hook)
- Route group `(auth)` provides the centered card layout; URL path is `/auth/verify` matching the `callbackURL` in both login and register pages
- TypeScript type check passes cleanly; build font-fetch errors are sandbox network restrictions only
- Files changed: `apps/web/src/app/(auth)/auth/verify/page.tsx` (new), `PLAN.md`

## Phase 1 — Root Routing Gate

### Task 18: Update app/page.tsx to branch on auth session
- Replaced hardcoded `const authenticated = true` with real server-side session check using `auth.api.getSession({ headers: await headers() })`
- Page is now an async server component; uses `headers()` from `next/headers` which also opts the page into dynamic rendering (no separate `connection()` call needed)
- When no session: renders `<Landing />` component with WPM branding, "Log in" (primary) and "Create account" (outline) CTAs linking to `/login` and `/register`
- When authenticated: renders existing `<RealtimeProvider>` + `<Dashboard />` + `<LiveTicker />` tree unchanged
- Created `apps/web/src/app/landing.tsx` — server component using shadcn Button (asChild with next/link), centered layout with `min-h-svh`
- TypeScript type check passes cleanly; build font-fetch errors are sandbox network restrictions only
- Files changed: `apps/web/src/app/page.tsx`, `apps/web/src/app/landing.tsx` (new), `PLAN.md`

## Phase 1 — Root Routing Gate (continued)

### Task 19: Add isAdmin helper and admin layout guard
- Added `isAdmin(session)` helper function in `apps/web/src/lib/auth.ts`
- Reads `ADMIN_EMAILS` env var, splits on comma, trims whitespace, checks if session user's email is in the list
- Returns `false` if no session or no `ADMIN_EMAILS` configured (fail-closed)
- Session type is minimal: `{ user: { email: string } } | null` — works with both server-side `auth.api.getSession()` results and client `useSession()` data
- Created `apps/web/src/app/admin/layout.tsx` — async server component that checks `isAdmin(session)` and calls `notFound()` for non-admins
- Uses `headers()` from `next/headers` to get the session server-side (same pattern as `page.tsx`)
- Non-admins (including unauthenticated users) get a 404 — intentionally reveals nothing about the admin route's existence
- TypeScript type check passes cleanly
- Files changed: `apps/web/src/lib/auth.ts`, `apps/web/src/app/admin/layout.tsx` (new), `PLAN.md`

## Phase 1 — Wallet Provisioning (lazy)

### Task 20: Create provisionWalletIfNeeded.ts
- Created `apps/web/src/lib/wallet/provisionWalletIfNeeded.ts` — lazy wallet provisioning function
- Accepts `{ id, walletPublicKey }` user object; returns immediately if wallet already provisioned
- Fresh registration: POSTs `{ userId }` to wpm-api `/api/register`, which creates a wallet and distributes the signup airdrop on-chain
- Recovery path: handles 409 (already registered) by falling back to `/api/wallet` lookup — covers the case where API-side keystore has the wallet but the local database doesn't
- Persists the returned `address` to the `user.walletPublicKey` column via direct `better-sqlite3` UPDATE
- TypeScript type check passes cleanly
- Files changed: `apps/web/src/lib/wallet/provisionWalletIfNeeded.ts` (new), `PLAN.md`
- Note: The wpm-api `/api/register` endpoint handles the airdrop distribution internally (`nodeClient.distribute`), so the caller just needs to await the response — no separate airdrop step needed

### Task 21: Wire provisionWalletIfNeeded into authenticated page path
- Updated `apps/web/src/app/page.tsx` — calls `await provisionWalletIfNeeded()` on the authenticated branch before rendering the dashboard
- Passes `session.user.id` and `session.user.walletPublicKey` from the better-auth session — better-auth's type inference correctly includes `walletPublicKey` from `additionalFields` config (no cast needed)
- The call is awaited so wallet provisioning (including the on-chain airdrop distribute) completes before the page renders — one-time cost for new users only; returns immediately when wallet already provisioned
- Covers both plan items: "Call provisionWalletIfNeeded from root layout server path" and "Verify airdrop lands on-chain before layout resolves (await the API response)"
- TypeScript type check passes cleanly; build font-fetch errors are sandbox network restrictions only
- Files changed: `apps/web/src/app/page.tsx`, `PLAN.md`

## Phase 2 — Landing & Dashboard Reads

### Task 22: Build Landing page per design brief
- Rewrote `apps/web/src/app/landing.tsx` — techno-brutalist landing page with halftone chief portrait hero
- **Halftone portrait**: Server-rendered SVG using a 44×56 dot-matrix grid where circle radii are computed from a mathematical brightness function. The function combines geometric primitives (ellipses, boxes, rotated rectangles) to form a recognizable chief silhouette: head, brow, nose, jaw, neck, shoulders, war bonnet feathers (13 radiating from head), and headband
- Uses `smoothstep()` SDF-style falloff for natural halftone gradation at shape edges; `hash()` function adds subtle per-cell noise for organic texture
- Sparse ambient dots (12% of empty grid cells) create a film-grain background effect outside the portrait
- **Typography**: Geist Mono (`font-mono`) for all text — `text-6xl/7xl/8xl font-black tracking-[0.25em]` title, `text-xs/sm uppercase tracking-[0.2em]` subtitle. Thin horizontal rule separates title from subtitle
- **Background**: Subtle repeating radial-gradient dot grid at 3% opacity for techno texture
- **CTAs**: "Log in" (solid) and "Create account" (outline) buttons with `uppercase tracking-wider font-mono` treatment, linking to `/login` and `/register` respectively
- All monochrome — uses only theme foreground/muted-foreground colors, no color
- Pure server component — no `'use client'` needed; SVG dots computed once at module level
- Single viewport layout (`min-h-svh`) per spec requirement
- TypeScript type check passes cleanly; build font-fetch errors are sandbox network restrictions only
- Files changed: `apps/web/src/app/landing.tsx`, `PLAN.md`
- Note: CTAs were already wired to `/login` and `/register` in Task 18; this task adds the design treatment

### Task 23: Cached data loaders
- Created 5 cached data loader files following the established `getMarkets()` pattern from `markets.ts`
- **`lib/data/market.ts`** — `getMarket(id)` fetches single `MarketWithOdds` from `/api/markets/:id`, tagged `market:${id}`, lifetime `minutes`
- **`lib/data/leaderboard.ts`** — `getLeaderboard()` fetches `LeaderboardEntry[]` from `/api/leaderboard`, tagged `leaderboard`, lifetime `minutes`
- **`lib/data/balance.ts`** — `getBalance(userId)` fetches balance via POST `/api/wallet` (returns `{balance, address}` directly, avoids extra token→balance hop), tagged `viewer:${userId}`, lifetime `minutes`. Exports `BalanceData` type
- **`lib/data/positions.ts`** — `getPositions(userId)` two-step: POST `/api/wallet` to get Bearer token, then GET `/api/positions` with auth header. Returns `SharePosition[]`, tagged `viewer:${userId}`, lifetime `minutes`
- **`lib/data/users.ts`** — `getUsers()` fetches from `/api/leaderboard` (no dedicated users endpoint exists yet), returns `UserEntry[]` (`{userId, address, balance}`), tagged `users`, lifetime `minutes`. Admin-only consumption
- All loaders use `'use cache'` directive with `cacheLife("minutes")` and `cacheTag(...)` from `next/cache`
- TypeScript type check passes cleanly
- Files changed: `apps/web/src/lib/data/market.ts` (new), `apps/web/src/lib/data/leaderboard.ts` (new), `apps/web/src/lib/data/balance.ts` (new), `apps/web/src/lib/data/positions.ts` (new), `apps/web/src/lib/data/users.ts` (new), `PLAN.md`
- Note: `getBalance` uses `/api/wallet` POST directly rather than the auth token → `/api/balance` flow because `/api/wallet` already returns balance in its response, saving a network round-trip
- Note: `getUsers` piggybacks on `/api/leaderboard` since no dedicated users API endpoint exists; the response shape (`{userId, address, balance}`) is sufficient for the admin users table

## Phase 2 — Dashboard Composition

### Task 24: Build Header server component with Balance and UserMenu
- Created `apps/web/src/components/header.tsx` — server component rendering logo (WPM link) + user menu in a `border-b` header bar with `max-w-screen-xl` constraint
- Contains async `BalanceLoader` server component (calls `connection()` + `getBalance(userId)`) wrapped in `<Suspense>` with "---" placeholder fallback
- Created `apps/web/src/components/balance.tsx` — client component receiving `initialBalance` and `address` props from server; subscribes to `balance:update` SSE events via `useRealtime()` hook, filters by matching `address`, and live-updates balance state
- Renders balance as `{balance.toLocaleString()} WPM` with `font-mono tabular-nums` for stable number width
- Created `apps/web/src/components/user-menu.tsx` — client component with user name display (hidden on mobile) and "Sign out" ghost button calling `authClient.signOut()` then `router.push("/")` + `router.refresh()`
- Updated `apps/web/src/app/page.tsx` — added `<Header user={...} />` above `<main>` in the authenticated branch; added layout styling (`mx-auto max-w-screen-xl px-4 py-6`) to `<main>` wrapper; removed the old `<h1>WPM</h1>` (now in header)
- TypeScript type check passes cleanly
- Files changed: `apps/web/src/components/header.tsx` (new), `apps/web/src/components/balance.tsx` (new), `apps/web/src/components/user-menu.tsx` (new), `apps/web/src/app/page.tsx`, `PLAN.md`

### Task 25: Build ScrollingLeaderboard component
- Created `apps/web/src/components/scrolling-leaderboard.tsx` — async server component fetching leaderboard via `getLeaderboard()`, sorted by balance descending
- Uses `connection()` from `next/server` to opt into dynamic rendering (consistent with other server components in the app)
- Renders ranked entries as inline items: rank number, name (or truncated address fallback), and balance — all in `font-mono text-xs`
- CSS-only marquee animation: duplicated content in `.marquee-track` with `@keyframes marquee-scroll` translating -50% over 30s linear infinite loop
- Fixed "Top" label on left edge with `z-10` and `bg-background` to stay visible as items scroll behind it
- Pauses animation on hover (`animation-play-state: paused`)
- Respects `prefers-reduced-motion: reduce` by disabling animation entirely
- Returns `null` when leaderboard is empty (graceful empty state)
- Added marquee CSS keyframes and utility classes to `globals.css`
- Updated `apps/web/src/app/page.tsx` — mounted `<ScrollingLeaderboard />` in `<Suspense>` between `<Header>` and `<main>` for full-width ticker effect
- TypeScript type check passes cleanly; build font-fetch errors are sandbox network restrictions only
- Files changed: `apps/web/src/components/scrolling-leaderboard.tsx` (new), `apps/web/src/app/globals.css`, `apps/web/src/app/page.tsx`, `PLAN.md`

### Task 26: Build Search client component
- Created `apps/web/src/components/search.tsx` — client component that filters the hydrated markets array
- Accepts `markets: MarketWithOdds[]` prop from server-rendered `<Dashboard />`
- Controlled text input (type="search") with `font-mono` styling, `max-w-sm` width constraint
- Filters on market name and both outcome names (case-insensitive substring match) using `useMemo` for performance
- Empty state: distinguishes between "no markets match your search" (with query) and "no active markets" (no markets at all)
- Renders filtered results as a simple list with market name + outcome probabilities (to be replaced by `<MarketList />` + `<MarketCard />` in the next task)
- Updated `apps/web/src/app/dashboard.tsx` — replaced inline `<ul>` market rendering with `<Search markets={markets} />`; added styled `<h2>` heading with `font-mono uppercase tracking-wider`
- TypeScript type check passes cleanly
- Files changed: `apps/web/src/components/search.tsx` (new), `apps/web/src/app/dashboard.tsx`, `PLAN.md`

### Task 27: Build MarketList, MarketCard, and LiveOdds components
- Created `apps/web/src/components/live-odds.tsx` — client component subscribing to `price:update` SSE events for a specific market via `useRealtime()` hook
- Renders two `OddsBar` sub-components (one per outcome): label, percentage, and a progress bar with animated width transition
- Filters SSE events by `marketId` match, updates `priceA`/`priceB` state on `price:update` events (same pattern as `<Balance />` component)
- Created `apps/web/src/components/market-card.tsx` — server-renderable card using shadcn `Card` primitives
- Displays market name (mono, bold), `<LiveOdds />` client leaf for real-time probabilities, footer with bettor count and time remaining
- `formatCloseTime()` helper renders human-readable countdown (e.g., "2d left", "3h 15m left", "45m left", "Closed")
- Highlights closing-soon markets (< 1 hour) in destructive color
- Created `apps/web/src/components/market-list.tsx` — renders a responsive grid of `<MarketCard />` components (`sm:grid-cols-2 lg:grid-cols-3`)
- Updated `apps/web/src/components/search.tsx` — replaced inline `<ul>` market rendering with `<MarketList markets={filtered} />`
- TypeScript type check passes cleanly
- Files changed: `apps/web/src/components/live-odds.tsx` (new), `apps/web/src/components/market-card.tsx` (new), `apps/web/src/components/market-list.tsx` (new), `apps/web/src/components/search.tsx`, `PLAN.md`

### Task 28: Build Portfolio server component
- Created `apps/web/src/components/portfolio.tsx` — async server component displaying user's share positions with P&L
- Uses `connection()` for dynamic rendering, fetches positions via `getPositions(userId)` and markets via `getMarkets()` in parallel
- Enriches positions by joining with market data: computes current value (`shares × currentPrice`) and unrealized P&L (`currentValue - costBasis`)
- Each position rendered as a `Card` showing: market name, outcome name (badge), share count, cost basis, current value, and P&L with color coding (green for gains, destructive for losses)
- Summary header shows total portfolio value and total P&L
- Empty state: "No positions yet. Place a bet to get started." when `positions.length === 0`
- Gracefully filters out positions whose market is no longer in the active markets list (resolved/cancelled markets)
- Updated `apps/web/src/app/page.tsx` — mounted `<Portfolio userId={session.user.id} />` in `<Suspense>` below `<Dashboard />` in the authenticated branch
- TypeScript type check passes cleanly
- Files changed: `apps/web/src/components/portfolio.tsx` (new), `apps/web/src/app/page.tsx`, `PLAN.md`
- Note: P&L is computed server-side from cached market prices; for real-time P&L updates, a future `useMarket` hook (Phase 5) would overlay live prices on the server snapshot

## Phase 3 — Market Detail & Mutations

### Task 29: Intercepting routes for market detail modal

- Created `apps/web/src/app/@modal/default.tsx` — returns `null` (required by Next.js parallel routes to prevent 404 on non-intercepted navigation)
- Created `apps/web/src/app/@modal/(.)market/[id]/page.tsx` — intercepting route that renders a Sheet side panel wrapping `<MarketDetail />` via composition pattern
- Page is a server component; `await params` pushed into `<MarketModalContent>` inside a `<Suspense>` boundary to satisfy PPR requirements
- Uses `<MarketSheet>` client wrapper (composition pattern: server component passes `<MarketDetail>` as children to client Shell, avoiding server-only import issues)
- Created `apps/web/src/components/market-sheet.tsx` — client component wrapping Sheet (Radix Dialog) from right side, `sm:max-w-lg`, closes via `router.back()`
- Created `apps/web/src/app/market/[id]/page.tsx` — canonical full-screen market page with back link and `<MarketDetail />` in `<Suspense>`
- Created `apps/web/src/components/market-detail.tsx` — async server component displaying market name, close status, outcome probabilities (`<LiveOdds />`), pool state (sharesA/B, liquidity, k), and payout multipliers. Will be extended with user positions and buy/sell controls in the next task
- Created `apps/web/src/components/ui/sheet.tsx` — standard shadcn Sheet component (Radix Dialog, CVA side variants: top/bottom/left/right, overlay with animations)
- Updated `apps/web/src/app/layout.tsx` — added `modal` parallel slot prop alongside `children`, renders `{modal}` after `{children}`
- Updated `apps/web/src/components/market-card.tsx` — wrapped Card in `<Link href={/market/${id}}>` to enable intercepting route navigation; added hover border transition
- Updated `apps/web/src/app/globals.css` — added `@import "tw-animate-css"` for Sheet slide-in/out and fade animations
- Added dependencies: `@radix-ui/react-dialog` (Sheet primitive), `tw-animate-css` (Tailwind v4-compatible animation utilities)
- TypeScript type check passes cleanly; `next build` compiles successfully (prerender failure on `/` is the known `better-sqlite3` native binding issue from Task 12, not related to this change)
- Files changed: `apps/web/src/app/@modal/default.tsx` (new), `apps/web/src/app/@modal/(.)market/[id]/page.tsx` (new), `apps/web/src/app/market/[id]/page.tsx` (new), `apps/web/src/components/market-sheet.tsx` (new), `apps/web/src/components/market-detail.tsx` (new), `apps/web/src/components/ui/sheet.tsx` (new), `apps/web/src/app/layout.tsx`, `apps/web/src/components/market-card.tsx`, `apps/web/src/app/globals.css`, `apps/web/package.json`, `bun.lock`, `PLAN.md`
- Gotcha: In Next.js 16 with `cacheComponents: true`, `await params` counts as uncached dynamic access and must be inside a `<Suspense>` boundary. Push the await into an inner async component (`MarketModalContent`) rather than awaiting in the page function directly
- Gotcha: Server components (using `cacheTag`, `connection()`, etc.) cannot be directly imported from client components. Use the composition pattern: the server component page renders a client Shell component and passes the server component as `children`

### Task 30: Build full MarketDetail with user positions and buy/sell controls
- Updated `apps/web/src/components/market-detail.tsx` — added optional `userId` prop; fetches positions via `getPositions(userId)` in parallel with market data; filters positions for the current market; renders "Your Position" section showing per-outcome shares, cost basis, and P&L; renders `<BetControls />` client component for trade UI
- Created `apps/web/src/components/bet-controls.tsx` — client component with Buy/Sell tab toggle, outcome selector (A/B buttons), amount input with estimated shares and payout preview (buy tab), shares-to-sell input with max button and estimated return (sell tab); buttons ready to be wired to server actions in the next task
- Updated `apps/web/src/app/@modal/(.)market/[id]/page.tsx` — gets session via `auth.api.getSession()` in the async `MarketModalContent`, passes `userId` to `<MarketDetail />`
- Updated `apps/web/src/app/market/[id]/page.tsx` — same session-based `userId` pass-through in the async `MarketPageContent` component
- Trade controls only render for authenticated users on open markets (`userId && !isClosed`); position section only renders when user has positions in this market
- Buy estimates: `shares = amount / price`, `payout = amount × multiplier`; Sell estimates: `return = shares × price`
- TypeScript type check passes cleanly; build font-fetch errors are sandbox network restrictions only
- Files changed: `apps/web/src/components/market-detail.tsx`, `apps/web/src/components/bet-controls.tsx` (new), `apps/web/src/app/@modal/(.)market/[id]/page.tsx`, `apps/web/src/app/market/[id]/page.tsx`, `PLAN.md`
- Note: The `@modal` parallel slot renders outside the `<RealtimeProvider>` context, so `<LiveOdds />` in the modal will display initial server-rendered prices without live SSE updates; Phase 5 will address this

### Task 31: Server Actions — placeBet, sellShares, and wiring to BetControls
- Created `apps/web/src/app/actions/placeBet.ts` — `'use server'` action that validates input with Zod v4 (`marketId: string`, `outcome: "A"|"B"`, `amount: number.positive()`), authenticates via `auth.api.getSession()`, fetches wallet token from wpm-api `/api/wallet`, POSTs to `/api/bet`, and calls `updateTag('market:${id}')` + `updateTag('viewer:${userId}')` on success
- Created `apps/web/src/app/actions/sellShares.ts` — same pattern: Zod validation (`shares` instead of `amount`), auth, wallet token, POSTs to `/api/sell`, same tag invalidation
- Both actions return `{ success: true }` or `{ error: string }` — no throwing, errors plumbed to UI
- Updated `apps/web/src/components/bet-controls.tsx` — wired Buy/Sell buttons to server actions via `useTransition` for non-blocking pending state. Added `isPending` disabled states on inputs/buttons, pending text ("Placing bet…"/"Selling…"), and inline success/error message display (green/destructive background). `marketId` prop now used (was previously accepted but unused)
- Added `zod@4.3.6` as dependency
- TypeScript type check passes cleanly
- Files changed: `apps/web/src/app/actions/placeBet.ts` (new), `apps/web/src/app/actions/sellShares.ts` (new), `apps/web/src/components/bet-controls.tsx`, `apps/web/package.json`, `bun.lock`, `PLAN.md`

## Phase 4 — Admin Surface

### Task 32: Admin shell — sidebar layout and overview page
- Updated `apps/web/src/app/admin/layout.tsx` — replaced bare fragment wrapper with full admin shell: header with "WPM / Admin" branding and "Back to app" link, responsive sidebar + main content layout
- Created `apps/web/src/components/admin-nav.tsx` — client component with sidebar navigation links (Overview, Users, Markets, System) using `usePathname()` for active state highlighting; exact match for `/admin`, prefix match for sub-routes
- Created `apps/web/src/app/admin/page.tsx` — overview page with 4 stats cards in a responsive grid, data fetched in parallel via `Promise.all`: Active Markets (open count + total), Signups Today (counts `Distribute` transactions with `signup_airdrop` memo from today's blocks), Chain Height (last block index + block count), Node Status (health indicator with green/red dot)
- Created `apps/web/src/lib/data/health.ts` — `getHealth()` cached loader fetching `/api/health`, tagged `health`, lifetime `minutes`. Returns `{ status, node }` with graceful fallback to `{ status: "degraded", node: false }` on fetch failure
- Created `apps/web/src/lib/data/blocks.ts` — `getBlocks()` cached loader fetching `/api/blocks`, tagged `blocks`, lifetime `minutes`. Returns `Block[]` from `@wpm/shared`
- Overview cards use `<Suspense>` with skeleton loading state (animated pulse placeholders)
- Admin guard (`isAdmin` check + `notFound()`) preserved from existing layout
- Sidebar hidden on mobile (`hidden md:block`), header always visible
- TypeScript type check passes cleanly; build font-fetch errors are sandbox network restrictions only
- Files changed: `apps/web/src/app/admin/layout.tsx`, `apps/web/src/app/admin/page.tsx` (new), `apps/web/src/components/admin-nav.tsx` (new), `apps/web/src/lib/data/health.ts` (new), `apps/web/src/lib/data/blocks.ts` (new), `PLAN.md`

### Task 33: Admin users page with distribute tokens action
- Created `apps/web/src/app/admin/users/page.tsx` — server component rendering a users table from `getUsers()` with per-row distribute tokens controls
- Async `UsersTable` component uses `connection()` for dynamic rendering, fetches users sorted by balance (descending, from leaderboard API)
- Table columns: rank (#), User ID (truncated), Address (truncated), Balance (formatted with toLocaleString), Distribute (inline form)
- Empty state: "No users registered yet." card when no users exist
- `<Suspense>` wrapper with skeleton loading fallback (5 animated pulse rows)
- Created `apps/web/src/components/distribute-form.tsx` — client component with amount input and "Send" button per user row
- Uses `useTransition` for non-blocking pending state, inline success/error message display
- Created `apps/web/src/app/actions/admin/distributeTokens.ts` — `'use server'` action that re-checks `isAdmin(session)`, validates input with Zod v4 (`userId: string`, `amount: number.positive()`), looks up wallet via `/api/wallet`, dispatches to `POST /api/admin/distribute` on wpm-api, invalidates `users` + `viewer:${userId}` cache tags
- Action follows established pattern from `placeBet.ts`/`sellShares.ts` (auth check, Zod validation, fetch, cache invalidation, `{ success }` / `{ error }` return type)
- TypeScript type check passes cleanly; build font-fetch errors are sandbox network restrictions only
- Files changed: `apps/web/src/app/admin/users/page.tsx` (new), `apps/web/src/components/distribute-form.tsx` (new), `apps/web/src/app/actions/admin/distributeTokens.ts` (new), `PLAN.md`
- Note: The distribute action calls `POST /api/admin/distribute` on wpm-api which doesn't exist yet — will need an API-side route that wraps `nodeClient.distribute()` for the action to work end-to-end

### Task 34: Admin markets page with resolve, cancel, and override seed actions
- Created `apps/web/src/app/admin/markets/page.tsx` — server component listing all markets with inline admin actions
- Async `MarketsTable` component uses `connection()` for dynamic rendering, fetches markets via `getMarkets()`, sorts by status (open → closed → resolved → cancelled)
- Each market card shows: name, outcomes, status badge (color-coded), close countdown (open markets), probabilities, pool liquidity, bettor count, and result (resolved markets)
- Active markets (open or closed) get three admin action sections: Resolve, Cancel, Override Seeds
- `<Suspense>` wrapper with skeleton loading fallback (3 animated pulse cards)
- Created `apps/web/src/components/market-admin-actions.tsx` — client component with three forms:
  - `<ResolveForm>` — two buttons (one per outcome) calling `resolveMarket` action; shows resolved outcome name on success
  - `<CancelForm>` — two-click confirmation pattern (click "Cancel" → button turns destructive "Confirm cancel" with "Nevermind" escape) to prevent accidental cancellations
  - `<OverrideSeedForm>` — two number inputs (Seed A, Seed B) + "Set" button calling `overrideSeed` action; clears inputs on success
- All three forms use `useTransition` for non-blocking pending state, inline success/error message display (same pattern as `<DistributeForm>`)
- Created `apps/web/src/app/actions/admin/resolveMarket.ts` — `'use server'` action, re-checks `isAdmin(session)`, validates with Zod v4 (`marketId: string`, `result: "A"|"B"`), dispatches to `POST /internal/resolve-market` on wpm-api (existing node endpoint), invalidates `markets` + `market:${id}` + `leaderboard` tags
- Created `apps/web/src/app/actions/admin/cancelMarket.ts` — same admin pattern, dispatches to `POST /internal/cancel-market`, invalidates `markets` + `market:${id}`
- Created `apps/web/src/app/actions/admin/overrideSeed.ts` — same admin pattern, validates `seedA`/`seedB` as positive numbers, dispatches to `POST /internal/override-seed`, invalidates `market:${id}`
- TypeScript type check passes cleanly
- Files changed: `apps/web/src/app/admin/markets/page.tsx` (new), `apps/web/src/components/market-admin-actions.tsx` (new), `apps/web/src/app/actions/admin/resolveMarket.ts` (new), `apps/web/src/app/actions/admin/cancelMarket.ts` (new), `apps/web/src/app/actions/admin/overrideSeed.ts` (new), `PLAN.md`
- Note: `cancelMarket` and `overrideSeed` actions target `POST /internal/cancel-market` and `POST /internal/override-seed` on the node, which don't exist yet — will need node-side routes. `resolveMarket` targets the existing `POST /internal/resolve-market` endpoint.

### Task 35: Admin system page — chain/oracle health panel with live event feed
- Created `apps/web/src/app/admin/system/page.tsx` — server component with four sections: node health, chain stats, live event feed, and recent blocks
- **HealthPanel**: Async server component fetching `getHealth()` — shows node connection status with green/red indicator and "Healthy"/"Degraded" label
- **ChainStats**: Async server component fetching `getBlocks()` — displays chain height, total transactions, block count, non-empty block count, and transaction breakdown by type (Distribute, CreateMarket, PlaceBet, etc.) sorted by frequency
- **RecentBlocks**: Async server component showing last 10 blocks (newest first) with block number, timestamp, hash (truncated), signer (truncated), and per-block transaction list with human-readable labels (e.g., "Bet 100 on A — abc123…", "Create 'Market Name'")
- Created `apps/web/src/components/system-event-feed.tsx` — client component with its own `EventSource` connection to `/api/stream` (separate from the main `RealtimeProvider` since admin pages render outside the dashboard's provider context)
- Listens for all three SSE event types (`price:update`, `market:resolved`, `balance:update`), maintains a rolling buffer of last 50 events
- Each event rendered with timestamp, color-coded type label (blue/yellow/green), and formatted detail text
- Shows connection status indicator and event count
- All server sections wrapped in `<Suspense>` with skeleton loading fallbacks
- Uses responsive `lg:grid-cols-2` layout for health + chain stats; full-width for event feed and recent blocks
- TypeScript type check passes cleanly; build font-fetch errors are sandbox network restrictions only
- Files changed: `apps/web/src/app/admin/system/page.tsx` (new), `apps/web/src/components/system-event-feed.tsx` (new), `PLAN.md`
- Note: The event feed opens its own EventSource rather than using `useRealtime()` because admin pages are not wrapped in `<RealtimeProvider>` (which lives in the authenticated dashboard branch of `page.tsx`)
